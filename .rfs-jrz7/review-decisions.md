# S1 review decisions

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | Public GithubAddress::parse bypasses the bounded PathReference parser; remove the unused alternate entry point | AddressingIncrementReview | Verified | Direct code check: github.rs:107 invokes parse_github_reference without validate_reference_input; the child accepts arbitrarily long unreserved path input. The existing public PathReference parser enforces MAX_PATH_REFERENCE_BYTES. LSP sibling-worktree query reports no references; independent grep finds no GithubAddress::parse calls or renamed imports in crates. | Accept | Remove the redundant public full-reference parser; retain PathReference as the full-reference boundary | No compatibility alias: this API is new, uncommitted and unused. |

## F1 atomic repair

- Ownership: C2; plan.md S1. Inherit grammar, canonicalization, budgets and module ownership unchanged.
- Root cause: a second public full-reference parser omitted the established whole-reference boundary. Remove GithubAddress::parse from core/src/reference/github.rs rather than duplicating validation or introducing a forwarding alias. No caller migration is necessary.
- Expected checks: core immutable-reference contracts retain exact65536 acceptance/65537 refusal and canonicalization; public-parser oracle remains39/39; workspace all-target compilation and placement census remain valid. Current full repository gate is allowed to finish on its unchanged snapshot before this edit.
- Evidence disposition: existing double-decode mutation remains applicable to the unchanged private segment decoder; decoded-control and placement mutations are unchanged. Parser/limit budget measurements exercise PathReference, not the removed method, and remain applicable. Refresh affected tests/compiler/placement after repair. Final S2/S3 integration, platform and live proof remain Main-owned and pending.
- Returned checkpoint result: PASS, all nine S1 gates reconciled in plan.md. F1 method removed. Post-repair full runner passed clippy, all-feature debug/release tests, all production budgets, placement and dependency vetting; its sole import-format failure was corrected and the exact formatting check passed. Other leg results remain valid after import-only formatting. qualification-s1.txt retains the exact disposition; no aggregate exit0 is fabricated.

# S1 review round 2 decisions (2026-09-10)

Verified against `origin/feat/rfs-jrz7` (5d941fb) before any edit; every row was re-checked on the repaired tree by the commands named below.

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| S1-R2-F1 | `maxDecodedBytes` is validated, intersected, schema-published and documented but no code compares a decoded byte count against it | code review (max) | Verified | No raise site at this head: the only consumers are `http/read.rs:333` (forwarding) and the validation tests; the enforcement site (`facts/source.rs:289`) exists only in S3. Caller-supplied values reach PR/comment Facts reads and do nothing. | Modify | Move the whole dimension to S3 (repair A); no increment advertises a control it cannot honor | Partition change under the approved design's item 8 interface; `budgeted-plan` owns the row move. The alternative (per-dimension capability rejection) is the "second controls object" the design forbids. |
| S1-R2-F2 | Six positional `Option` parameters in `ReadAcquisitionLimits::new` allow transposition | code review (max) | Verified (three adjacent byte limits already positionally ambiguous at five arguments) | `crates/resourcefs-core/src/acquisition.rs:19`; the only production caller names every field at the MCP boundary (`mcp/src/acquisition.rs:38`). | Reject | N/A — no change | The approved design pins this interface ("extend existing constructor ... no second controls object or compatibility constructor"); the implied named-field wrapper is that second object and needs a design amendment, not a review repair. Body-level transposition is caught by the distinct-value contract tests (mutation-c6-controls.txt precedent). |
| S1-R2-F3 | The checked-in standalone gate `module_shape_base.py` fails on its own branch (2044 > 2010) | code review (max) | Verified | `python scripts/module_shape_base.py --stage query` exits 1 with 52 failures; the same command on `main` already exits 1 with 41, so the entry point was a superseded-baseline trap, and the reference.rs ceiling item is the one this branch added. CI runs only `scripts/module_shape.py` (green). | Modify | Retire the standalone entry (repair C) and pin the ceiling in the base policy | The one checked-in placement entry is `scripts/module_shape.py`; the base module is loaded as a library by it. |
| S1-R2-F4 | `claim = 'C11' if 'immutable-grammar' in policy.active else 'C02'` inside the single `fail()` sink relabels every failure as C11 | code review (max) | Verified | `module_shape.py:195-197`; live probe: a mutated transport getter printed `C11 FAIL .../http/mod.rs: last_modified ...` before the repair. | Modify | Per-check claim argument (repair C): C11 only for immutable-grammar owner rows and protected-parent checks; inherited failures no longer prefixed `C02 ` | Transfer/placement evidence can again name the claim it actually falsifies. |
| S1-R2-F5 | The `protected_parents` loop resolves a per-path baseline with `git show` and no existence or revspec check | code review (max) | Verified | `module_shape.py:414`; live probe: `"baseline": "main"` passed the gate and `main~1` produced 19 spurious failures. | Modify | Validate a full lowercase commit id and probe it with `cat-file -e` before use (repair C) | A missing object still fails closed; it now fails with the row that caused it rather than as an anonymous `git show` abort. |
| S1-R2-F6 | Owner-uniqueness only flags duplicates under the owner's own parent directory | code review (max) | Verified | `module_shape.py:301`; live probe: a verbatim copy of `parse_github_reference` in `crates/resourcefs-mcp/src/acquisition.rs` passed the old rule and fails the new one. | Modify | Compare declaration bodies workspace-wide instead of names within one directory (repair C) | Name-only widening fires 9 false positives over this tree; the identical-body rule fires none and catches the relocation this gate exists to prevent. |
| S1-R2-F7 | New `parent_limits` and `wiring_calls` ledger keys relax the gate unclamped and permanently | code review (max) | Verified | `module-ledger.json:406-410`, `module_shape.py:139-157`; live probe: raising the ceiling to 999999 was accepted. | Modify | Delete `parent_limits` (the ceiling lives in the pinned base policy, as r31i did) and validate `wiring_calls` shape (repair C) | Cumulative stage semantics are pre-existing for `body_changes`/`added_functions`; the unbounded numeric relaxation was the new hazard and is gone. |
| S1-R2-F8 | `ResourceAddress::Github(_)` is folded into the GitHub-record arms, so an unimplemented family's category depends on whether a source is mounted | code review (max) | Verified | `compiled.rs:132,170,323` at this head; mounted → `unsupported_projection`, unmounted → `source_unavailable`. | Modify | Give the family its own arms (repair B); S2 moves the read arm back to routing when commit Facts exist | Fences: `compiled_sources_contract.rs` (unmounted) and `github_adapter_contract.rs` (mounted) assert the same category. |
| S1-R2-F9 | `GithubSource::read` calls `reject_acquisition` before inspecting the address, so one resource returns two reasons | code review (max) | Verified | `github/mod.rs:823-832`; reason `acquisition_controls_unsupported` versus the projection error. | Reject | N/A — no change | Subsumed by repair B: `CompiledSources` no longer routes the unserved family to the adapter at this increment, and the controls-first order matches the pinned issue:// and pr:// convention (`github_adapter_contract.rs:241-254`). |
| S1-R2-F10 | `github://` is routed but advertised nowhere (catalog, DESIGN.md, CONTEXT.md, operating.md) | code review (max) | Verified as fact, resolved by the stack | `catalog_entries` returns issue:///pr:// at this head; P13 adds the `github://` commit row plus DESIGN/CONTEXT/operating rows, P14 the source rows. | Reject | N/A — no change at S1 | `compiled.rs:57-59` states the rule the finding ignores: a build must not advertise a scheme no reference can resolve — and after repair B this increment resolves none. |
| S1-R2-F11 | The only change to the frozen `github/mod.rs` is an inserted blank line | code review (max) | Verified | `git diff main -- .../github/mod.rs` is one insertion, whitespace-only. | Accept | Delete the blank line (repair F) | The file is now byte-identical to main, which is what a protected parent's diff should show. |
| S1-R2-F12 | `parse_github_reference` accepts a `ProjectionSelector` on `/facts`, minting references the canonical-identity validators reject | code review (max) | Verified; intended by the approved behavior | Design: "Source selectors are not another content API: as with existing singular facts, projections on these facts fail unsupported_projection". Precedent: `facts.rs:579-580` for `pr://` facts; adapters return the unprojected canonical reference (`facts.rs:599` at S3). | Reject | N/A — no change | The validators define canonical *source identity*; parse-admit/read-refuse is how every remote family spells "not a content API". |
| S1-R2-F13 | `validate_decoded_segment` admits backslashes and control bytes, so a Windows-separator traversal survives | code review (max) | Refuted as a defect; the behavior is approved | Design: "Unicode, spaces, backslashes and representable control bytes are encoded, not normalized"; fence `immutable_segment_constructors_preserve_git_filename_bytes` pins it; no consumer maps segments to a filesystem or an HTTP path (S3 compares them with Git tree entry names and builds URLs from object IDs). | Reject | N/A — no change | Git names are byte strings; rejecting backslash would corrupt valid paths. `validate_percent_encoding`'s encoded-separator rule is filesystem containment and is applied by the families that need it. |
| S1-R2-F14 | `%252F` survives as a literal `%2F` segment, so a second decode reconstructs `/` | code review (max) | Refuted as a defect; the behavior is approved and fenced | Design: "A literal `%2F` filename uses `%252F`"; `immutable_addresses_expose_typed_commit_and_decoded_source_identity` asserts the decoded identity; the design's C2 named mutation is exactly the double-decode case. | Reject | N/A — no change | Decoding happens once at the parser boundary; `segments()` is documented and consumed as literal bytes. |
| S1-R2-F15 | `GithubSourcePath::new` bounds neither segment count nor total length, bypassing the 64 KiB guard | code review (max) | Verified | `github.rs:46-70`; the whole-reference ceiling runs only in `PathReference::parse`. Sibling convention: `JiraQuery::new` enforces the same ceiling. | Modify | Enforce the reference ceiling where the typed value is built, and presize the canonical string (repair D) | A typed path that cannot fit a Path Reference can no longer exist; the boundary test asserts inclusive acceptance and refusal one byte over. |
| S1-R2-F16 | Non-canonical percent spellings are rewritten instead of refused, unlike Jira | code review (max) | Verified as fact; the behavior is approved and fenced | Fence `immutable_segment_canonicalization_is_single_pass_and_uppercase` accepts `%7e/%41/%3a` and asserts the canonical spelling; the C1 witness accepts alternative spellings; `issue://`/`pr://` canonicalize identically. | Reject | N/A — no change | Jira's segment-canonicality rule is Jira's own; here canonical *emission* is the approved rule, and the identity guards' tautology is the pre-existing shape for five other families. |
| S1-R2-F17 | `GithubCommitId::new` re-spells a 40-hex rule that `sha()` in the sources crate already enforces | code review (max) | Verified (duplication), different layers | `reference/github.rs:15` versus `sources/src/github/facts/identity.rs:389`; plan.md:168 explicitly declined sharing. | Reject | N/A — no change | The two validators live in different crates with different error vocabularies (invalid reference versus upstream malformed); a shared public core predicate for one call site is not worth the surface, and the recorded plan decision already covers it. |
| S1-R2-F18 | `is_unreserved` and the encoder's inline alphabet are two copies that must agree | code review (max) | Verified | `github.rs:218` duplicated `reference.rs:1801` after this branch hoisted the encoder. | Accept | Keep one predicate beside the encoder and use it from both (repair D) | The parser's accepted-literal set is now the encoder's emitted set by construction. |
| S1-R2-F19 | The canonical path is rebuilt with an unpresized `String::new()` where Jira presizes | code review (max) | Verified | `github.rs:42-70`; `jira.rs:341,359,372` presize. | Accept | Presize from the decoded byte total (repair D) | Removes the grow-and-copy on the branch's own construction path. |
| S1-R2-F20 | `canonical_reference()` allocates inside both identity guards | code review (max) | Verified; pre-existing shape | `resource.rs:642-645`, `discovery.rs:1258-1261`; Catalog/Local/Issue/PullRequest/Https arms allocate identically. | Reject | N/A — no change | The conjunct is defense-in-depth whose allocation is one small string per resource or discovery hit; removing it for one family deletes a guard, and making it allocation-free needs a different approved address representation. |
| S1-R2-F21 | `immutable_reference_parse_budget` is registered as a production budget but its bounds cannot bite and it silently passes in debug | code review (max) | Verified for the bounds and the debug no-op; refuted for "can never fail" (heap assertion holds 4.7x headroom) | Release measurements: 225837ns parse average, 14ns control average; bounds were 1s and 1ms; `if cfg!(debug_assertions) { return; }`. | Modify | Fail loudly in debug and tighten to 5ms parse / 1µs control (repair E) | The heap bound stays; the sibling small-reference budget keeps its 1ms for a 183-byte input. |
| S1-R2-F22 | `PathReference::github` has no callers or tests and can return `Err` for already-typed input | code review (max) | Verified for callers/tests; the `Err` case is a genuine reference ceiling | Zero callers at this head; plan.md records that family callers arrive with the typed family (S3 `facts.rs:599`); the Err case needed a path that cannot be referenced. | Modify | Keep the family-consistent constructor, bound typed paths (repair D) and add the round-trip fence | The constructor is the seam S3 consumes; the fence pins requested-equals-canonical round-tripping. |
| S1-R2-F23 | Two comments still claim `percent_decode` requires prior `validate_percent_encoding` | code review (max) | Verified | `reference.rs:1047-1048`, `1602-1605`; this branch made `percent_decode` reject its own malformed escapes. | Accept | Correct both comments to the current contract (repair D) | The family list is now accurate (issue://, pr://, jira://, github:// own their syntax). |

## S1 review round 2 repairs

- **Repair A — decoded-control repartition (F1).** Root cause: the control surface landed one increment before the only code that can compare a decoded byte count against it. Change: `core/acquisition.rs`, `core/error.rs`, `mcp/acquisition.rs`, the generated profile-schema fixture, `http/read.rs`, `github/facts/collection.rs`, their tests and the DESIGN.md row and limit-kind word leave S1 and land in S3 with `facts/source.rs`. Ownership: design item 8 (unchanged interface); plan.md S1/S3 rows updated by the plan owner. Expected: no increment advertises an unenforceable control; `scripts/ci-gates.py` and the focused crates pass at this head.
- **Repair B — unserved-family refusal (F8, F9, F10).** Root cause: one match arm served two different facts about the address (which adapter owns it, and whether that adapter is mounted). Change: `compiled.rs` read/resolve/search own arms plus `github_family_unreadable()`; fences in `compiled_sources_contract.rs` and `github_adapter_contract.rs` assert the same category with and without a mounted source and with and without caller controls.
- **Repair C — placement-gate policy (F3–F7, plus two verified extras).** Root cause: claim labelling keyed on the active stage set, ledger relaxations unbounded and silent on typos, baselines taken as revspecs, and owner uniqueness scoped to one directory. Change: per-check claims, pinned ceilings in `module_shape_base.py` (2010 → 2080 for `core/src/reference.rs`), `parent_limits` deleted, `wiring_calls` shape validation, staged-key stage validation, pinned-baseline validation, workspace-wide identical-body ownership comparison, and a library-only base module. Probe evidence: mutation-review2.txt.
- **Repair D — grammar type bounds and one alphabet (F15, F18, F19, F22, F23).** `GithubSourcePath::new` enforces the reference ceiling and presizes its canonical string; `is_unreserved` moves beside the encoder and is shared; the two stale comments are corrected; the typed round-trip and ceiling fences are added.
- **Repair E — budget honesty (F21).** Release-only by refusal instead of silence, with bounds at 5 ms/1 µs against measured 225837 ns/14 ns.
- **Repair F — diff hygiene (F11).** The whitespace-only hunk in the protected parent is gone.
- Evidence disposition: C1/C2 parser mutations remain applicable (the segment decoder and its refusals are unchanged); the decoded-control mutation moved to S3 with the dimension; the placement mutation is re-proved against the repaired gate in mutation-review2.txt; the routing, ceiling and duplicate-ownership mutants are new and recorded there. Final S2/S3 integration, platform and live proof remain Main-owned and pending.

# S1 review round 3 decisions (2026-09-10)

Source: `docs/research/pr12-code-review-2026-09-10.md` (automated review, 15 reported
findings plus a below-the-line inventory), verified against `origin/feat/rfs-jrz7`
(6e1c55c) before any edit and re-checked on the repaired tree. Six read-only scouts
re-derived every mechanism from source; the decisive rows were re-proved by the
mutation A/B in `mutation-review3.txt` (reviewed gate at 6e1c55c versus the repaired
gate, same injected policy row) and by release budget runs.

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| S1-R3-F1 | Typed `GithubSourcePath::new` bounds only the encoded path against 64 KiB, ignoring the `github://…/facts` envelope, so accepted values cannot become a `PathReference` | code review (max) | Verified | The envelope is 65 + owner + repo bytes (67..=204): `GithubAddress::canonical_reference` plus `MAX_GITHUB_OWNER_BYTES`=39 / `MAX_GITHUB_REPOSITORY_BYTES`=100 (`reference.rs:386-387`). At the reviewed head every canonical path length in 65,333..=65,536 can be paired with an identity that refuses it, and the 65,536-byte construct was asserted to succeed. Review's "74" is the test identity's own framing and "65,332" is off by one (that length fits even the longest identity). | Modify | Repair D2: bound the typed path by the *shortest* reference framing (65,469 = 64 KiB − 67), measured exactly before materialization, and answer `limit_exceeded` as the whole-reference boundary does; fence `typed_source_path_ceiling_is_exact_for_the_shortest_reference` derives that bound from the shortest admissible identity. | The reviewer's implied fix (reserve the *largest* envelope, 204) would make `PathReference::parse` refuse a 64 KiB reference the approved boundary accepts (`immutable_reference_retains_existing_64k_boundary`); the shortest framing is the sound bound for "addressable by some repository", and the per-repository ceiling stays at the reference, fenced by `whole_reference_ceiling_survives_a_path_some_repository_can_address`. |
| S1-R3-F8 | The parser stores a projection on `/facts` while both identity validators reject every projected `github://` reference, and no test asserts either behavior | code review (max) | Refuted as a defect; Verified as a coverage gap | Design pins it ("projections on these facts fail `unsupported_projection`", design.md:18) and the real precedent is the singular `pr://` Facts document: `github/facts.rs:576-581` refuses a projection at read while `github_reference_contract.rs:472-490` pins the parse side. The two probe artifacts answer different questions — `falsify_grammar.py:32-35` models a *reader* refusing a reference that does not end `/facts`, `compare_grammar.py` compares the production parser — so they do not contradict each other. | Modify | Repair F: `facts_documents_admit_a_selector_the_read_path_refuses` pins parse-admit plus projection presence for `:raw`, a line range and `:page:`. | The read half stays unobservable while the family is unserved; it lands with S2/S3 (rfs-jrz7). A Facts document is singular, so `:page:` is a request the reader refuses, not a record identity. |
| S1-R3-F11 | `github://` is the only path-bearing family that never calls `validate_percent_encoding`, so an encoded backslash survives into a validated segment | code review (max) | Verified | `parse_encoded_segment` calls `percent_decode` directly; the four pre-validating call sites are `reference.rs:1050/1282/1608` and `jira.rs:370`; `segments()` has no consumer outside core tests. | Modify | Repair D2: the `GithubSourcePath` type doc now states that segments are Git tree entry names to compare with a tree listing and never to join to a filesystem or cache path. | Behavior unchanged: backslash and control-byte preservation is the approved decision (S1-R2-F13), fenced by `immutable_segment_constructors_preserve_git_filename_bytes`. The review's own "latent, not exploitable" reading is the one that holds. |
| S1-R3-F13 | The capacity hint sizes the encoded buffer from the decoded total and the ceiling is checked only after the whole canonical string is materialized | code review (max) | Verified | Reviewed head: `String::with_capacity(decoded_bytes)` at `github.rs:56` before a 3x-expanding encoder, ceiling at `:66` after the loop. Measured on the repaired tree (release, 100 iterations of the 64 KiB reference, back-to-back runs on one machine): 736 µs with the exact count versus 652 µs when the count is replaced by the decoded hint — the count costs ~11% at the ceiling and removes the escape-driven regrowth. | Modify | Repair D2: the exact canonical length is computed in the segment loop (shared `is_unreserved`), refusal precedes materialization, and the encode is presized exactly. | The review's "both fixes are free" is wrong: the byte count costs one extra pass. Its absolute timings (1.748 ms / 707 µs / ~10 ms) were not reproduced as such; the byte arithmetic is what was re-derived. |
| S1-R3-F2 | The mounted half of the paired family fence passes with and without the arms it exists to protect | code review (max) | Verified | The fold mutation leaves the mounted read answering the adapter's `unsupported_github_projection` (`github/mod.rs:1157`), which is `UnsupportedProjection` with `details() == None` — byte-identical to the fence's two assertions. The fixture router's panic runs in a detached connection task (`tests/support/tls.rs:508`), so a served request cannot fail the test. | Modify | Repair E: the mounted fence asserts the compiled registry's refusal message for read, controls-bearing read, resolve and search; the controls read and the resolve assertions the unmounted sibling had are added. | The mutation arm only became indistinguishable-by-category after repair C aligned it with the read; the message is now the observable that separates "compiled registry refuses the family" from "adapter refuses the projection". |
| S1-R3-F9 | The C19 fence's rationale cites the `expect`-based panic contract this PR deleted, and the family that now decodes without the pre-pass has no row | code review (max) | Verified | The `expect`s are `ok_or_else` in `percent_decode` (`reference.rs:1772-1799`); `%GG`, `%C3`, `%00`, `%2F`, `%2E%2E` and backslash/control bytes are already pinned by the new core contract, so the genuinely unpinned form is a malformed escape on this family. | Accept | Repair F: rationale rewritten to the current contract plus a `github://…/notes%.md/facts` row (RejectsMalformed). | The review's wider claim ("nothing pins it") is too strong; the core immutable contract already covers the other github forms. |
| S1-R3-F14 | Nothing bounds the segment count and the budget hardcodes the cheapest segment shape, so the parser's real worst case is never measured | code review (max) | Verified | Reviewed head: `immutable_reference_with_size` fixed at 1,024 segments and both assertions calibrated on it; the adversarial shape (32,731 one-byte segments) is constructible under the same 64 KiB ceiling. Release measurements on the repaired tree: cheap shape 0.65–1.03 ms / 449,657 B; adversarial 4.4–5.9 ms / 2,194,430 B (idle vs loaded machine, same build). | Modify | Repair G: the budget measures both shapes; the adversarial bounds are 30 ms wall and `64 * EXACT_BYTES` heap (2x the measured structural cost), with the release figures recorded beside them. | No segment-count ceiling added: the 64 KiB reference ceiling already bounds a reference at ~32.7k segments, the measured amplification is ~33 heap bytes per input byte, and a new hard limit is a `DESIGN.md` behavior decision rather than a review repair. |
| S1-R3-F3 | The mutation arm advertises `github://` as read-only while the read arm says the same family is not served | code review (max) | Verified | `compiled.rs:172-175` answered `UnsupportedMutation` / "github:// Resources are read-only…" against `:132`'s `UnsupportedProjection` / "…not served by the compiled sources"; the `https`/`jira` arms it is modelled on are truthful because those families are readable when mounted. | Modify | Repair C: the mutation arm returns `github_family_unreadable()`, and both fences assert the mutation refusal is the read refusal. | The permanent immutable-target refusal belongs to S2/S3 (`design.md:109`, `github/mutation.rs`); at S1 the honest answer is the family refusal. |
| S1-R3-F4 | The obsolete-helper check keys on old names while `historical_owners` is rekeyed to new names, so a pure ownership move is unsatisfiable | code review (max) | Verified | A/B M1 (`mutation-review3.txt`): a row moving the policy-tracked `fetch_bounded_attempts` to its own approved owner makes the reviewed gate fail "obsolete shared helper … must migrate to its approved owner"; the repaired gate passes. | Modify | Repair A: a row that keeps the name is a pure move, so only a declaration outside the approved owner fails; a row that renames retires the old name anywhere. | A/B M4 and M5 show the retirement and placement failures are unchanged, so nothing was relaxed. |
| S1-R3-F5 | `relocated_counts` only counts declarations inside the historical watch list, so an owner elsewhere reports "found 0" | code review (max) | Verified | A/B M2: a relocation owned by `crates/resourcefs-core/src/discovery.rs` passes the repaired gate because the census now reads every `crates/*/src` file; the reviewed census counts zero there and a zero count is a failure. | Modify | Repair A: the one-owner census reads all production sources, masking only files that name a relocated symbol. | The same change retires the `unknown historical owner symbol` abort (A/B M2/M3), which mislabelled a ledger defect as C02 oracle input. |
| S1-R3-F6 | The ledger-driven `git show` for a protected parent is unguarded, so a path absent at a valid pin aborts the whole gate | code review (max) | Verified | A/B M6: a `protected_parents` row naming a path that exists now but not at `635ab170` makes the reviewed gate abort `C02 FAIL oracle input: git show 635ab170…:…/github.rs failed (exit 128)` and abandon every other check; the repaired gate reports `C11 FAIL …: protected parent is absent at its pinned baseline …`. | Accept | Repair A: the path read is `optional=True` and fails the row instead of the run. | — |
| S1-R3-F7 | Owner uniqueness counts declarations in raw source while the sibling census uses production-masked source, so an inline `#[cfg(test)]` module produces a false failure | code review (max) | Verified (latent) | A/B M7: appending a multi-line `#[cfg(test)] mod` copy of an owned symbol to its owner file makes the reviewed gate fail "owned symbol … must have exactly one declaration"; the repaired gate passes. No inline test module exists in `crates/` at this head. | Modify | Repair B: owner uniqueness reads production-masked spans, so a test module is not an ownership site. | The review's one-line probe string would not have fired the line-anchored span pattern; the multi-line form does. |
| S1-R3-F10 | Owner uniqueness narrowed to name-plus-identical-body loses divergent copies and trait declarations, and re-parses files per symbol pair | code review (max) | Verified | A/B M8: a divergent same-name copy in the owner's directory passes the reviewed gate and fails the repaired one. A/B M9: a `trait` bearing an owned name in the owner file now also reports "must have exactly one declaration". Whole-gate time 24.9 s → 21.1 s with the memoised census. | Modify | Repair B: the directory-scope name rule is restored *additively* (ahead of the missing-symbol `continue`, which also restores T2's diagnostic), `trait` joins the span pattern, and spans are memoised per path. | A straight revert would lose this PR's own named C11 mutation (a verbatim cross-crate copy), which only the identical-body rule detects. |
| S1-R3-F12 | The pinned policy still owns `encode_jira_segment`, a symbol this PR deleted, and the correction lives in a runtime ledger patch | code review (max) | Verified | `HISTORICAL_OWNERS` mapped the deleted symbol to `jira.rs`; the correction applied only when `transition is not None`, which the gate always passes but the function defaults to `None`. | Modify | Repair A: the pinned policy records current ownership (`encode_rfc3986_segment → reference.rs`); the ledger row expresses only the migration, and the policy-known/unknown distinction no longer gates a row. | — |
| S1-R3-F15 | The `wiring_calls` ledger table is an allowlist compensating for `CALL` matching `Github(` inside `ResourceAddress::Github(_)` | code review (max) | Verified | `CALL` captures any identifier before `(`; the reviewed gate without the allowlist reports `new_calls=['Github']` for `discovery.rs::canonical_identity` (first A/B run). `read_query` is a genuine new call in a changed parent body, so only its half is a workaround. | Modify | Repair A: variant and constructor paths are excluded from call detection, retiring the `wiring_calls` table, its shape validator and the `Query` hardcode. | The review's "retires both workarounds" is wrong for `read_query`, which stays allowed. |
| S1-R3-T1 | Both Identity arms reject every `github://` projection while Issue/PullRequest admit a `:page:` continuation | code review (max) | Verified for the Github clause; Refuted for the parity clause | `resource.rs` requires `projection().is_none()` for Issue/PullRequest too, and Jira's arm requires it before `jira_record_identity`; commit/source Facts are singular documents with no pagination (design.md:18,45). | Reject | N/A — no change | No reachable consequence while the family is unserved; revisiting the continuation model is S2/S3 work under rfs-jrz7. |
| S1-R3-T2 | The new `continue` suppresses the cross-file "belongs in" diagnostic | code review (max) | Verified | The reviewed rule reported only "required owned symbol … is missing" once a symbol left its owner. | Accept | Repair B: the namespace rule runs before the missing-symbol `continue`, so both diagnostics are reported. | — |
| S1-R3-T3 | A bad `relocated_symbols` row raises before ledger-shape validation, mislabelling a ledger defect as C02 | code review (max) | Verified (A/B M2/M3 reproduce the reviewed abort) | The raise keyed on the pinned policy's symbol list, so a row for any symbol the policy does not track aborts the run. | Accept | Repair A: rows are authoritative for their migration; unknown-name aborts are gone, and the A/B rows show the failure surfaces as a C1 claim where one applies. | — |
| S1-R3-T4 | The `reference.rs` tripwire was over-raised and its ceiling is written twice | code review (max) | Verified as fact; not a defect | The ceiling is plan-recorded policy (raised 2010 → 2080 in repair C of round 2); the file is 2053 lines, and the "before" 2089 figure is the archived baseline count. | Reject | Repaired the duplication only: `HISTORICAL_PARENTS` now derives its maximum from `PARENTS`, so one table states each ceiling. | Tightening the ceiling is a plan-owner update; no defect today. |
| S1-R3-T5 | `ResourceAddress` is a public enum without `#[non_exhaustive]` in a workspace pinned at 1.0.0 | code review (sweep) | Verified | No crate carries `publish = false` and no enum in `crates/` is `#[non_exhaustive]`. | Reject | N/A — no change | Repo-wide pre-existing pattern; a versioning policy decision, not this increment's, and there is no external consumer of the workspace crates. |
| S1-R3-T6 | Asymmetric normalization between the repo half and the SHA half | code review (angle D) | Verified | `GithubRepositoryIdentity::new` lowercases both parts; the SHA validator admits only lowercase hex. | Reject | N/A — no change | Deliberate and fenced (`immutable_grammar_rejects_aliases_and_unrepresentable_operands`); immutable identity is canonically lowercase. |
| S1-R3-T7 | `GithubAddress` exposes public struct-variant fields, so no invariant can attach to the combination | code review (angle E) | Verified | Three call sites treat `canonical_reference()` as an identity that must re-parse. | Reject | N/A — no change | Private fields plus validating constructors are an interface change on a public type (needs design approval); F1's hole is closed by the bound the path type can own, and the residual is fenced. |
| S1-R3-T8 | `github://` is parseable but described by no `SourceCatalogEntry` | code review (angle E) | Verified as fact; approved behavior | `GithubSource::catalog_entries` lists `issue://` and `pr://`; `compiled.rs:57-59` states a build must not advertise a scheme no reference can resolve. | Reject | N/A — no change | The catalog/DESIGN/CONTEXT rows land with the increments that serve the family (plan P13/P14, rfs-jrz7); already dispositioned as S1-R2-F10. |
| S1-R3-T9 | A core-parser budget lives in a sources HTTP contract test binary | code review (angles C, F) | Verified for placement; the comparability premise is refuted | `immutable_reference_parse_budget` measures `PathReference::parse` yet lives in `github_facts_contract.rs`; the sibling `reference_parse_budget` has no heap counter at all, so there is nothing to compare against. | Reject | N/A — no change | Moving the row would relocate the counting allocator into the core contract binary and change the runner's budget registry; F14's substance is fixed in place. |
| S1-R3-T10 | The C11 claim label is keyed off one hard-coded stage name | code review (angle I) | Refuted | plan.md assigns S2 no C11 obligation (C3/C9), and S3's C11 obligation is the protected-parent check, which already labels `C11` unconditionally. | Reject | N/A — no change | — |
| S1-R3-T11 | `protected_parents` introduces a third pinned baseline | code review (angle I) | Verified as fact | Three pins answer "did an untouched body change?" against three trees: base `BASELINE`, `ledger['baseline']`, and the per-row protected-parent pin. | Reject | N/A — no change | Folding the hard-coded `GITHUB` parent into the generic loop is a restructure of two different baselines' checks; the row exists for S2/S3. |
| S1-R3-Q1 | `GithubCommitId::new` re-spells the 40-hex rule `facts/identity.rs::sha` already enforces | code review (angle F) | Verified (duplication), different layers | Two spellings in two crates with different error vocabularies. | Reject | N/A — no change | Already recorded as S1-R2-F17: the two validators answer different failures. |
| S1-R3-Q2 | `parse_encoded_segment` and the re-encode duplicate Jira's segment codec | code review (angle F) | Verified (three copies), not a defect | `jira.rs` performs a canonicality equality check github does not, deliberately (S1-R2-F16). | Reject | N/A — no change | Hoisting the codec would change Jira's pinned canonicality rule; the shared alphabet is already single-sourced. |
| S1-R3-Q3 | `validate_decoded_segment` is the third copy of the single-segment rule | code review (angle F) | Verified (three validators, three rules) | `LocalName::new`, `validate_jira_opaque_segment` and this one differ in their extra rules. | Reject | N/A — no change | Each family's extra rule is its own contract; a shared validator would have to parameterize all three and would blur the traversal rule. |
| S1-R3-Q4 | `parse_github_reference` duplicates the "split selector, parse address" shape | code review (angle F) | Verified (third copy), style only | `jira.rs:386-395`, `pull.rs:42-49`, `github.rs:156-171`. | Reject | N/A — no change | The three differ in post-validation (F8); factoring now would re-open a rejected design question. |
| S1-R3-Q5 | The `github://` prefix strip duplicates `parse_github_body` | code review (angle F) | Verified | Both emit the same malformed-reference message. | Reject | N/A — no change | `parse_github_body` is parameterized for the record families and ends at a different stage; sharing the strip alone buys little. |
| S1-R3-Q6 | The ceiling answers `invalid_reference` where the module's `limit_exceeded` names the same failure | code review (angle F) | Verified | `validate_reference_input` returns `limit_exceeded` for the same constant; `DESIGN.md` uses `limit_exceeded` for every size ceiling. | Accept | Folded into repair D2: the typed-path ceiling now answers `limit_exceeded`. | `JiraQuery::new` still answers `invalid_reference` for its ceiling — pre-existing and outside this increment. |
| S1-R3-Q7 | `immutable_reference_with_size` rebuilds a reference the core contract already builds | code review (angle F) | Verified (two builders) | The sources budget and the core contract each derive a maximum-size reference. | Reject | N/A — no change | The sources row measures the parse through its own allocator; a shared builder would cross a test-binary boundary. |
| S1-R3-Q8 | The stage-roster validation repeats the same block per section | code review (angles F, G) | Verified | Three identical row-stage blocks plus one stage-key block and a second section loop in `Transition.__init__`. | Accept | Repair A: one loop over `(section, label)` pairs; the retired `wiring_calls` section is gone with its validator. | — |
| S1-R3-Q9 | `GithubAddress::repository()/commit()/path()` have no call sites | code review (angle G) | Verified | No production or test call site; the enum's variants are public, so field access needs no accessor. | Accept | Repair D2: the three accessors are deleted. | Consistent with the round-2 deletion of the unused `GithubAddress::parse`. |
| S1-R3-Q10 | `GithubSourcePath` carries two orderings and hashes both fields | code review (angle G) | Verified | `PartialOrd`/`Ord` have no consumer anywhere; `canonical` is injective in `segments`. | Modify | Repair D2: `PartialOrd`/`Ord` are dropped from `GithubSourcePath` and `GithubCommitId`; equality/hash stay derived. | Hand-delegating `PartialEq`/`Hash` to `canonical` changes no observable answer, so it is left for a consumer that measures it. |
| S1-R3-Q11 | `parse_github_address` holds three constructs at once | code review (angle G) | Verified, readability only | Two spellings of `invalid_reference` no test distinguishes. | Reject | N/A — no change | The grammar is fenced by the contract table; rewriting it to fewer branches buys style and risks the fences. |
| S1-R3-Q12 | Six copies of the `PathReference` struct literal | code review (angle G) | Verified | One literal per remote family branch in `reference.rs::parse`. | Reject | N/A — no change | A private constructor in the protected parent is a parent restructure with no behavioral content; the field set is stable. |
| S1-R3-Q13 | `before_nodes` is assigned and never read | code review (angle G) | Verified | The `GITHUB` call site uses `after_nodes` only; the protected-parent site discards both. | Accept | Repair B: `check_parent_nodes` returns only `after_nodes`. | — |
| S1-R3-Q14 | The base module's `main()` and `__main__` guard are unreachable | code review (angle G) | Verified (unreachable), not a defect | The module is loaded only by importlib from `module_shape.py`. | Reject | N/A — no change | The guard is the retired-entry message decided as repair C of round 2 (S1-R2-F3); `main()` raises, so the `SystemExit` behaviour is unchanged. |
| S1-R3-Q15 | Three micro-findings: a dead stage guard, a re-tested `transition`, a diff-noise blank line | code review (angle G) | Verified (each is inert) | `module_shape.py:450` cannot fire (`policy.active ⊆ known_stages`); the relocation comprehension re-tests a condition four lines after its guard. | Reject | N/A — no change | Inert guards are not defects; the relocation block was rewritten wholesale by repair A anyway. |
| S1-R3-Q16 | `canonical_reference()`'s `format!` has no capacity hint | code review (angle H) | Verified as measured behavior | 6 allocations / 196,732 bytes for a 64 KiB source address. | Reject | N/A — no change | The review's own fix requires storing the canonical string in `GithubAddress`, an interface decision on a public type; a capacity-hinted `format!` would re-spell the framing arithmetic the tests now pin. |
| S1-R3-Q17 | `remote_requested` evaluates its default eagerly, copying the canonical reference | code review (angles A, H) | Verified | `projection.map_or(base.clone(), …)` copies then drops for every projection-less parse on four families. | Reject | N/A — no change | The review itself notes this fix must land with Q16 or it permanently retains the over-allocated buffer; both are deferred together rather than half-applied. |
| S1-R3-Q18 | Identity checks allocate a full canonical reference to compare | code review (angle H) | Verified | The arm is copied from the hot Issue/PullRequest arms; pre-existing shape. | Reject | N/A — no change | Already recorded as S1-R2-F20; an allocation-free comparison needs a different approved address representation. |
| S1-R3-Q19 | `validate_percent_encoding` and `percent_decode` scan the same bytes twice | code review (angles H, I) | Verified as fact | Both passes exist on the workspace path; the callee's malformed-escape branch is reachable from the family that does not pre-validate. | Reject | N/A — no change | Folding the separator rule into the decoder changes a shared helper's contract for six call sites; the pass is cheap and pre-existing. |
| S1-R3-Q20 | The `cat-file -e` probe in the protected-parent loop is redundant | code review (angle G) | Refuted | `mutation-review2.txt:41-42` shows the probe localizing an unresolvable pin to its own row ("protected parent baseline … is unresolved"); without it the same input surfaces as the anonymous failure class F6 just closed for the path read. | Reject | N/A — no change | The review's own F6 fix is to guard the third read, not to drop the first two. |
| S1-R3-Q21 | Derive family servability from the source registry instead of three hard-coded arms | code review (angle I) | Verified as an altitude observation; the proposed mechanism is wrong | Registry-derived servability is exactly the mount-dependent category the round-2 repair B removed. | Reject | N/A — no change | The three arms are the mount-independent refusal; what the family needs at S2/S3 is an explicit arm change, not inference. |
| S1-R3-Q22 | Replace the seven prefix branches in `PathReference::parse` with a family dispatch table | code review (angle I) | Verified as counted | `parse` has 7 prefix branches; one family touched 11 exhaustive matches. | Reject | N/A — no change | A protected-parent restructure with no behavioral content; it belongs to a placement increment, not a review repair. |
| S1-R3-Q23 | Collapse the duplicated ceiling constant | code review (angle I) | Verified | The same ceiling appeared in `PARENTS` and `HISTORICAL_PARENTS`. | Accept | Repair A: `HISTORICAL_PARENTS` derives its maximum from `PARENTS`. | — |
| S1-R3-Q24 | `relocated_symbols` is a fourth ownership table with a narrower scope | code review (angle I) | Verified | A JSON table that patches a Python constant. | Modify | Repair A: the pinned policy owns current ownership and the ledger owns migrations only; the census is workspace-wide. | F4/F5/F12 were its consequences and are resolved. |
| S1-R3-Q25 | The ticket-store update rode in the first of nine commits | code review (angle J) | Verified as fact | `git log 635ab17..6e1c55c -- .rivets/issues.jsonl` returns one commit. | Reject | N/A — no change | The note is a progress record for an increment still open; the round-3 record rides this repair's commit. |

## S1 review round 3 repairs

- **Repair A — placement policy and gate mechanism (F4, F5, F6, F12, F15, T2, T3, Q8, Q23, Q24).**
  Root cause: the relocation mechanism conflated the ledger's two keyspaces, the one-owner
  census was scoped to the historical watch list, the pinned policy carried a deleted symbol,
  and call detection read enum-variant patterns as function calls, which forced a ledger
  allowlist. Change: `module_shape_base.py` (policy ownership, relocation semantics,
  workspace-wide census, variant-aware `calls()`, derived `HISTORICAL_PARENTS` ceilings),
  `module_shape.py` (deduplicated roster validation, guarded pinned read, retired
  `wiring_calls` validator) and `scripts/module-ledger.json` (table retired). Proof:
  `mutation-review3.txt` — nine A/B cases against the reviewed gate, with the retirement and
  placement failures unchanged where they must bite.
- **Repair B — owner census (F7, F10, T2, Q13).** Root cause: owner uniqueness read raw
  source, compared bodies only, and re-parsed files per symbol pair. Change:
  `module_shape.py` reads production-masked spans, restores the directory-scope name rule
  ahead of the missing-symbol `continue`, adds `trait` to the span pattern and memoises spans
  per path; `check_parent_nodes` returns the census its caller uses. Proof: A/B M7–M9 plus the
  whole-gate time (24.9 s → 21.1 s).
- **Repair C — one capability claim per address (F3, F2).** Change: `compiled.rs` mutation arm
  returns the family refusal; `compiled_sources_contract.rs` and `github_adapter_contract.rs`
  assert the same refusal, category and message, for read, controls-bearing read, resolve and
  search, mounted and unmounted.
- **Repair D2 — typed path bound and identity surface (F1, F11, F13, Q6, Q9, Q10).** Change:
  `reference/github.rs` measures the exact canonical length before materialization, bounds it
  by the shortest reference framing, answers `limit_exceeded`, documents the segment
  containment rule, and drops the unused accessors and orderings; `reference.rs` is unchanged
  apart from the earlier round-2 edits. Fences: `typed_source_path_ceiling_is_exact_for_the_shortest_reference`,
  `whole_reference_ceiling_survives_a_path_some_repository_can_address`,
  `immutable_reference_retains_existing_64k_boundary`.
- **Repair E — mounted fence parity (F2).** See repair C; the assertion additions are the
  proof obligation F2 named.
- **Repair F — grammar and guard coverage (F8, F9).** Change:
  `github_immutable_reference_contract.rs` gains the selector fence;
  `path_reference_contract.rs` gains the current rationale and the github malformed-escape row.
- **Repair G — parser budget honesty (F14).** Change: `github_facts_contract.rs` measures the
  cheapest and the adversarial segment shape with bounds derived from release measurements
  (746 µs / 449,657 B and 4.58 ms / 2,194,430 B at this revision).

Evidence disposition: the C1/C2 grammar mutations and the decoded-control mutation remain
applicable (the segment decoder and its refusals are unchanged apart from the exact-length
measurement). The placement mutation is re-proved for the repaired gate in
`mutation-review3.txt`. The parser budget's bounds are re-derived from fresh release
measurements because its fixture changed. The routing/ceiling fences changed assertion text
only where the corrected behavior requires it (repair C) or the fixture was wrong (F2). Final
S2/S3 integration, platform and live proof remain Main-owned and pending.
## S2 incremental review decisions

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| S2-F1 | Account URL construction retains an empty Enterprise API path segment | CommitBoundaryReview | Verified | Real SourceAdapter fixture under `/api/v3/` failed with `upstream_identity_mismatch`; the other nine immutable cases passed. | Accept | Remove the trailing empty segment before appending encoded path components; append the entire account route with one URL clone. | The Enterprise fixture now passes with exact prefixed HTTP targets and returned account URL. LSP could not index this sibling-worktree helper; scoped reference search found only its local definition/calls before the rename. |
| S2-F2 | Optional links mask the named requested-SHA equality mutation | CommitBoundaryReview | Verified | The compiled equality mutant left the original WrongSha test green. | Accept | Omit optional SHA-bearing commit links only in the wrong-SHA fixture and its valid-SHA positive control; retain the required correct API URL. | The same equality mutant now fails and restored source passes; `mutation-c3-commit.txt`. This repairs oracle isolation, not the production expectation. |

S2 integration corrections also removed an independently duplicated SHA validator in favor of `identity::sha`, corrected native Presence field accesses and macro imports, confined hostile native login text to URL path segments, validated supplied nested Git Data commit links, and removed the unnecessary parent-ID uniqueness restriction and mirror dispatch enum. The existing parent/owner census now recognizes the existing `native!` declaration macro and explicitly permits only the three additional reused identity helper entry points at the immutable-commit stage.

Source immutable contracts: ten pass after S2-F1/F2 repair. Real-binary stdio overflow/recovery contract passes. Credential-gated adapter and stdio commit rows both ran against the pinned public commit and passed without skipping. C3 and C9 compiled mutants fail; exact source restoration passes. Final full-gate and S2 publication judgment remain in plan.md.

## S2 review round 2 record (2026-09-10)

This branch rebases onto the repaired S1 tip, so the S1 round-2 decision table above applies unchanged. Only what this increment's own changes carry is recorded here.

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| S1-R2-F8, S1-R2-F10 | The unserved-family refusal belongs to the increment that has no implementation; this one has commit Facts | code review (max) | Verified at S1, resolved here | Commit Facts read through the compiled registry and the real binary; `.rfs-jrz7/mutation-c3-commit.txt` and the stdio contract cover the served route. | Accept | Route `ResourceAddress::Github(_)` reads to the configured source again; keep the discovery and mutation refusals configuration-independent | The paired fences move with the increment: an unmounted build reports the missing profile entry for a read and the family refusal for discovery and mutation; a mounted registry never fetches for discovery. S3 adds the source route with no change here. |
| S1-R2-F6 (macro form) | Owner uniqueness must recognize the declaration form this increment introduced | code review (max) | Verified | `native!(NativePerson {..})` generates a type with no declaration span; the workspace-wide comparison now takes the macro invocation as the subject. | Modify | Compare macro invocations like declarations | Probe: a verbatim copy of `native!(NativePerson {..})` in another family fails; a differently shaped same-named invocation stays green. |

## S3 review round 2 record (2026-09-10)

This branch rebases onto the repaired S1 and S2 tips, so their round-2 tables apply unchanged. This section records what this increment's own changes carry.

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| S1-R2-F1 | The decoded cap must exist where a decoded byte count does | code review (max) | Verified at S1; landed here | `facts/source.rs` compares the verified tree-entry size against the effective cap and reports `decoded_content_bytes` with its bound; `mutation-c6-controls.txt` records the intersection mutant red (exit 101) and restored green (exit 0). | Accept | The dimension, its MCP field, generated schema, error vocabulary, docs row and contract tests land with the enforcing code | The interface is the one S1 approved — spelling, lower-only rule, 4 MiB hard default and `decoded_content_bytes` kind unchanged; only the increment changed, so no increment ever advertised a control it could not honor. |
| S1-R2-F10 | The family must be advertised where it resolves | code review (max) | Verified | The catalog entry, DESIGN.md sections and the operating guide rows for commit Facts (S2) and source Facts (this increment) describe the addresses the configured source serves. | Accept | Advertise with the implementing increment | `compiled.rs`'s own rule — never advertise a scheme no reference can resolve — is what makes the sequencing visible in the catalog output. |
## PR #13 review repair round (2026-09-11)

Review document: `docs/research/pr13-code-review-2026-09-11.md`; the re-review of this round is `docs/research/pr13-rereview-2026-09-11.md`. F1–F13 and T1–T14 are the review's own identifiers. Code repairs are implemented in this round by the parent (`crates/`) and the gate agent (`scripts/`); the documentation and evidence rows (F4, F11, T13) are repaired in `.rfs-jrz7/*`, `DESIGN.md` and `docs/operating.md` by this round's documentation workstream.

| finding-id | decision | note |
|---|---|---|
| F1 | Repair | Stop rebuilding account links from the login: accept the provider's own route family (`/users/dependabot%5Bbot%5D`, `/apps/<slug>`) so app/bot-authored commits read, with a bot-shaped fixture. |
| F2 | Repair | Restore main's all-span iteration over same-named counterpart declarations in the owner-uniqueness rule. |
| F3 | Repair | The account-presence fence asserts key presence before `is_null()`, so its null half can fail. |
| F4 | Repair | The deterministic stdio row keeps its deliberate harness route; the evidence sentences it outran are corrected in `evidence.md` and `qualification-s2.txt` (this round). |
| F5 | Repair | Split presence from identity so an absent required link no longer answers `not_found`. |
| F6 | Repair | Delete `ParentLinks` in favour of the shared `facts::Links`. |
| F7 | Repair | Reword the family-refusal sentence and doc comment to the claim the family can make. |
| F8 | Repair | Add a commit-route per-call acquisition-controls fence mirroring the PR row's effective-`maxAttempts` case. |
| F9 | Repair | Serve or refuse the unserved `…/source/…` spelling at the same boundary as its siblings. |
| F10 | Repair | Bind the family decision where the address is first destructured instead of re-deriving it. |
| F11 | Repair | Correct the recorded claims: control-route list (`docs/operating.md:186`), adapter count and S2 checkpoint counts (`qualification-s2.txt`, `plan.md`), family index (`DESIGN.md:127`); the live-row sub-item is T13. |
| F12 | Repair | A `.`/`..` login can no longer vanish from the expected URL. |
| F13 | Repair | Add a `cap + 1` case so the commit budget row notices a loosened ceiling, not only a tightened one. |
| T1 | Repair | Consolidate the duplicated `macro_rules! missing` against `facts/pull.rs`'s copy. |
| T2 | Accepted, deferred | The new contract binary did copy its sibling's transport/mount helpers and native JSON builders; consolidating them into a GitHub-family support module is a test-fixture refactor whose churn would invalidate this round's per-fence bit-proofs, and the review's own verifier narrowed the claim (the finder's proposed `tests/support/jira.rs` home is not reusable, and the shared TLS/session support is already reused). Deferred to the increment that next edits those fixtures. |
| T3 | Accepted, deferred | Fix needs a lifetime-parameterized DTO layer and a placement-gate accommodation; output bytes, semantics and the registered budgets are unaffected. |
| T4 | Repair | Record the WrongSha row's deliberate fixture omissions so re-adding links cannot silently re-mask the equality mutant. |
| T5 | No change | Kept as a defensive read, not because it is currently reachable: no caller passes a path absent from `codes` (owner paths are checked against `sources` before the loop and counterpart paths come from `declarations`, built from `codes`), so the guard cannot fire today and its removal would be behaviour-preserving. It stays because it makes both reads of the same map answer absence the same way — `production_nodes` tolerates a path with no production code and returns `{}`, while this raw-text read would raise `KeyError` and surface as an unlocated `C02 FAIL oracle input` instead of a named failure. Removing it would make the raw-text read stricter than the span read of the same map, which is the asymmetry the guard exists to prevent. |
| T6 | Repair | Correct the docstring (`declaration_subjects` now returns one subject per same-named production span) to state that a subject is the span's token stream, since its input is production-masked. |
| T7 | No change | Refuted: ignored-row registration is complete and unclassified rows fail the gate. |
| T8 | No change | Pre-existing and previously dispositioned; the increment changes the message's truth, not the category. |
| T9 | No change | The runner always executes budgets with `--exact --test-threads=1`; the hazard is a local-invocation false red in the fail-safe direction. |
| T10 | No change | The generation rule's single owner is already `facts.rs` (`establish_generation`/`check_generation`); `accept_generation` is a 6-line family-local composition of the two, and hoisting it would leave the four sibling inline comparisons untouched while spending owner line budgets already near their ledger maxima (`facts.rs` at 679 of 700 at this head, 671 at the S2 tip), for no behavioural gain. |
| T11 | Repair | Extract the GitHub source row both live profiles mount into one builder. The two envelopes stay separate: sharing them would need a builder taking the file name and cache directory as adjacent string arguments, which is the positional-transposition hazard the repository rejects elsewhere. |
| T12 | No change | Benefit refuted: the peak doubling is not caused by escape density and the registered escape-dense row already exercises the fragmented path. |
| T13 | Repair | Correct the live-row non-skip claim (this round): the receipt cannot distinguish a real run from a skip; the recorded ~2 s durations are the only receipt-side signal. |
| T14 | No change | Substance held; only the documentation attribution was overstated. |

## PR #14 review repair round (2026-09-11)

Review document: `docs/research/pr14-code-review-2026-09-11.md`. Head reviewed and repaired: `882b4ef` (branch `feat/rfs-jrz7-source`). Fifty findings, all dispositioned below; five candidates the review itself refuted and eleven items it verified clean are not re-litigated. Verification ran as seven read-only scouts clustered by file, and every load-bearing claim (F1, F2, F5, F6, F24, F29) was re-proved by the parent from source or by executing the premise.

Repairs are grouped by atomic change. Every behavior-changing repair was verified by rebuilding the workspace and running the full repository gate green (`python scripts/ci-gates.py`, "All repository gates passed"), and each new fence was shown to fail on an injected fault before being accepted.

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | A blob-fetch deadline refusal is classified as retainable, so a deadline refusal is published as a successful partial Facts document. | PR #14 review (max) | Verified | Mechanism traced end to end at 882b4ef: `retry_wait_fits` is a strict `<` (http/mod.rs:140-143), so a 429 with Retry-After beyond the remaining deadline returns `deadline_error` = SourceUnavailable/DeadlineExceeded with the deadline NOT expired (http/read.rs:250-258); `ordinary_blob_failure` was `!invalidates_retention(..) && !UpstreamMalformed`, and `invalidates_retention` named only Cancelled/PermissionDenied/UpstreamIdentityMismatch (collection.rs:248), so the refusal was converted to `content.state:"unavailable"` and the trailing `check_acceptance` passed with ~29s left. | Modify | Repair R1: `retains_verified_metadata` in `facts/source.rs` is now an explicit allowlist of ordinary request failures; DeadlineExceeded, any LimitExceeded, UpstreamMalformed, UpstreamIdentityMismatch, CacheGenerationChanged and Cancelled refuse the whole read. Fence: `source_facts_refuse_the_whole_read_for_a_deadline_refusal` drives 429 + `Retry-After: 120` and asserts category SourceUnavailable, reason DeadlineExceeded, retryGuidance DelaySeconds(120), and no document. | The reviewer proposed no fix. Their natural lever — editing the shared `invalidates_retention` — would also change ratified collection semantics (DESIGN.md:240), so the source family got its own policy instead. |
| F2 | One attempt per path component against an unraisable 10-attempt ceiling makes a deep path permanently content-less rather than refused. | PR #14 review (max) | Verified | Attempt arithmetic confirmed against the PR's own fixtures: repository + commit + d trees + blob = d + 3, all from one `HttpReadBudget` whose ceiling core validates lower-only (core/acquisition.rs:109). `deep/d1/d2/d3/d4/d5/d6/payload` (8 components) spends exactly 10 and the blob's 11th charge failed with LimitExceeded, which the funnel then published as `acquisition_failed`. Reviewer's illustrative 8-component path (`src/main/java/com/example/app/Service.java`) is actually seven components and succeeds; also, a non-blob terminal spends no blob attempt, so the bound holds only for regular-file terminals. | Modify | Repair R1 + R9: attempt exhaustion is a typed whole-read refusal (category LimitExceeded, reason limit_exceeded, limit.kind attempts, bound 10, observed 11) exactly as a 9-component path already was; DESIGN.md and docs/operating.md now state the derived bound and that the allowance is lower-only. Fixture `deepBlobLimit` and its generator now expect `error`/`limit_exceeded`; `source_facts_keep_one_attempt_budget_through_deep_blob_stage` asserts the typed refusal. | The reviewer's fix was "no-fix-proposed"; the scout's construction-time depth ceiling was not applied because that couples the core address grammar to a transport budget and is a design change. The residual depth limitation is filed as rfs-lqat. |
| F3 | `MAX_TREE_ENTRIES = 1000` is an un-tunable hard constant applied to every tree, reported under a kind with no caller-facing field, and it has no test. | PR #14 review (max) | Refuted | The bound is covered by a discriminating contract case in both directions: the shared fixture defines 1000-entry (`wide`, asserted `available`) and 1001-entry (`tooWide`, asserted reason LimitExceeded) tree templates, generated by `generated_tree`, and `source_facts_cover_every_independent_fixture_case` asserts both. The cited reporting path (`describe_limit_rejection`) is reached only for caller-written `acquisition` syntax, not for a read failure, which renders through `render::failure` into `details.limit{kind,bound,observed}`. The bound is documented at DESIGN.md:226 and docs/operating.md:240. | Reject | N/A — no change. | The residual true fact — the ceiling cannot be raised — is a deliberate local-policy decision and is recorded under F34. |
| F4 | The pre-fetch decoded-size refusal is driven by the tree entry's `size`, which the reconstructed tree SHA does not cover, and that unverified number is then published as an observation. | PR #14 review (max) | Verified | `gix_object`'s tree writer emits `mode SP name NUL oid` and nothing else, so a Git tree entry carries no size (vendored gix-object 0.64.1 tree/write.rs); `validate_tree` builds `Entry { mode, filename, oid }`, so the API's `size` is outside the hash. The refusal at source.rs ran before the blob GET, so `validate_blob`'s exact-size cross-check never ran, and `decoded_limit(ctx, terminal.size_bytes)` published that number as `limit.observed`. | Modify | Repair R3: the supplied-size pre-refusal now emits `limit.observed` absent — an unverified upstream number is not presented as a measurement — while `data.sizeBytes` still carries the supplied metadata the contract names. Fence: `source_facts_refuse_a_size_refusal_without_claiming_an_observed_size` over-states an entry size for `run.sh` and asserts the refusal, the absent `observed`, and that no blob was requested. | Keeps the approved design decision (`design.md` item 4: a reported oversized size skips the blob) while removing the false observation. |
| F5 | The same funnel retains LimitExceeded/response_body_bytes, publishing partial Facts JSON where DESIGN says response/admission/representation overflow fails without publishing partial Facts JSON. | PR #14 review (max) | Verified | `maxResponseBytes` is an independent legal caller dimension; a 4 MiB decoded blob encodes to 5,592,408 bytes, so a caller response ceiling of 4 MiB trips `response.truncated()` (fetch.rs:306-329) returning LimitExceeded + LimitDetail{response_body_bytes}, which `ordinary_blob_failure` retained. DESIGN.md:378 states the opposite in the same PR. | Modify | Repair R1. Fence: `source_facts_refuse_the_whole_read_when_the_response_ceiling_bounds_the_blob` drives the exact decoded-boundary corpus under `maxResponseBytes = 4_194_304` and asserts category LimitExceeded, reason limit_exceeded, limit.kind response_body_bytes, and no document. | Understated in the review: attempts and accepted_body_bytes raised during the same fetch were swallowed identically; the allowlist now refuses every LimitExceeded. |
| F6 | The same funnel is over-broad in the other direction: a PermissionDenied on the blob fetch rejects the whole read and discards already-verified terminal metadata. | PR #14 review (max) | Verified | `classify_facts_status` maps 401, and 403 without a rate-limit signal, to PermissionDenied/UpstreamDenied; `invalidates_retention` matched PermissionDenied, so `read_blob`'s `?` discarded the verified terminal identity, while an identical 503 preserved it. The source section's refusal list (DESIGN.md:230) does not include permission denial. | Modify | Repair R1: `retains_verified_metadata` treats PermissionDenied/UpstreamDenied as an ordinary failure of that one request, so the verified identity and bounded failure facts are published. Fence: the retention test covers 503, 401 and 404 with their exact published (category, reason) pairs. | The review's word "contradicts" was overstated, as F6's own verification showed: the preservation clause is permissive ("may"). The behaviour is now chosen deliberately and stated in DESIGN.md. |
| F7 | `maxDecodedBytes` is advertised on every acquisition-bearing read but accepted-and-silently-ignored by PR/comment/review/inline/commit Facts. | PR #14 review (max) | Refuted | DESIGN.md:374 — two lines above the rule the finding cites — documents exactly this: existing PR/comment Facts perform no content decoding and retain their five-dimensional envelope. The dimension's deferral is a recorded decision (review-decisions.md S1-R2-F1), whose note expressly forbids the per-dimension capability rejection the finding implies. | Reject | N/A — no code change. The one documentary residue is repaired under R9: DESIGN.md:374 now also names immutable commit Facts, which likewise emits five dimensions. | The reviewer quoted DESIGN.md:376 but not :374. |
| F8 | Tree and blob `identity::require_object_link` calls omit the presence pre-check `commit.rs` performs, so an absent or null link yields not_found/upstream_identity_mismatch where the design mandates malformed. | PR #14 review (max) | Verified | `require_object_link`'s `_` arm catches both absent and null (`Presence::value()` is None for Omitted and Null alike), while `identity::required` returns UpstreamMalformed; commit.rs does the presence check first and documents why. Neither `validate_tree` nor `validate_blob` requires the top-level `url`, so an omitted url reached only the link guard. | Accept | Repair R2: `identity::required(&tree.url)?` and `identity::required(&native.url)?` precede both required-link checks. Fence: `Mutation::OmitTreeUrl` is a row of the verdict table asserting SourceUnavailable/UpstreamMalformed. | The design sentence cited is in the commit section; for source links the rule is by analogy to commit.rs, which the reviewer's own verifier noted. |
| F9 | Tree entry names round-trip through a JSON String, so one non-UTF-8 filename fails the whole tree and blames the provider. | PR #14 review (max) | Verified | `native!` wraps the entry path in `Presence<String>`, `VerifiedEntry.name` is a `String`, the name is hashed via `BString::from(name.as_bytes())`, and the GitHub tree endpoint must carry the name as a JSON string. | Reject | N/A — permanent non-goal. No implementable change exists: JSON cannot represent a non-UTF-8 name, so the bytes are either laundered (hash mismatch) or the body is not valid UTF-8 (malformed). The entry is also unaddressable by construction, since `GithubSourcePath` segments are `String`s. | The reviewer's retry framing does not hold on this path: `retryable` is consulted only by the collection engine. |
| F10 | An intermediate path component that resolves to a blob, symlink or submodule returns source_unavailable/upstream_malformed where the sibling missing-name case returns not_found. | PR #14 review (max) | Verified | The descent branch returned `failure(ErrorReason::UpstreamMalformed)` eleven lines after the missing-name branch returned NotFound/UpstreamNotFoundOrHidden; the tree had just passed full SHA reconstruction, so nothing was malformed, and the profile layer buckets SourceUnavailable as an I/O error. | Accept | Repair R2: a non-directory ancestor now returns NotFound/UpstreamNotFoundOrHidden through the shared `path_absent()` helper. Fence: `source_facts_classify_mid_path_and_absent_components_as_not_found` covers `run.sh/anything`, `link/anything`, `submodule/anything` and the absent-name case, with the request count pinning that the refusal happens at the last tree. | Applies to `disposition != Tree`, so symlink and submodule ancestors are covered, not only blobs. |
| F11 | `validate_optional_object_link` fails the read on an unparseable or query-bearing entry url — the opposite of its doc comment and of three sibling validators. | PR #14 review (max) | Verified | The doc promises "opaque provider values stay lossless" but `matches_object_url` used `Url::parse(...).is_ok_and(...)`, so an unparseable value landed in the same arm as a foreign object; `validate_optional_comment_link` and `validate_observed_link` both return Ok for an unparseable value. | Accept | Repair R2: the optional validator now returns Ok for a value that is not a URL at all and judges a value that is a URL on authority and path only, matching the sibling rule that a query or fragment spelling does not change which object is named. Fence: `source_facts_read_a_path_whose_sibling_entries_carry_opaque_links` covers opaque and query-bearing entry urls as both sibling and selected entry. | With R2's selected-entry narrowing, a sibling's link is no longer consulted at all; both halves are fenced. |
| F12 | `github.source` always publishes `unavailableFacts: []` and collapses Presence::Null with Presence::Omitted for `sizeBytes`. | PR #14 review (max) | Verified | `unavailable_facts: Vec::new()` was a literal with no `skip_serializing_if`, and `size_bytes: entry.size.value().copied()` mapped Null and Omitted alike to None with the key dropped either way; siblings keep `Presence<T>` under `skip_serializing_if = "Presence::omitted"` plus a `missing!` record. | Accept | Repair R3: `SourceData.size_bytes` is a `&Presence<u64>` under `skip_serializing_if = "Presence::omitted"`, `Terminal.size_bytes` carries the presence through, and the document records the null/omitted case through the shared `missing!` path. Fence: `source_facts_publish_presence_aware_terminal_size` asserts the omitted key plus the matching `unavailableFacts` entry with reason `omitted`. | The reviewer's DESIGN citation ("absent, null and present metadata remain distinct") is scoped to commit Facts; the source section promises sizeBytes "when supplied". The consistency defect is real; the contract-violation framing was stronger than the text. |
| F13 | The largest synchronous CPU work in the adapter runs on the async task with no spawn_blocking, against the repo's own convention. | PR #14 review (max) | Verified | Sixteen production `spawn_blocking` sites exist across seven files; none in the new path. Base64 decode, SHA-1 over 4 MiB and tree reconstruction all ran inline inside the polled future, and `tokio::time::timeout` cannot pre-empt synchronous code because `Timeout::poll` polls the inner future first. | Modify | Repair R5: `validate_tree` and `validate_blob` now run on `tokio::task::spawn_blocking`, with a JoinError mapping in the same shape as the repo's other worker helpers. The representation serialization stays inline — `Facts` borrows locals — and that is recorded here rather than papered over. | `block_in_place` was not used: it panics on the current-thread runtime that plain `#[tokio::test]` uses. Magnitude in the debug gate was not measured; the mechanism is what the convention requires. |
| F14 | `pause_c8_serialization` blocks for up to 5s inside `GlobalAlloc::realloc` and performs re-entrant allocation there, in a process-wide allocator. | PR #14 review (max) | Verified | The pause site is called from `realloc`, sends on a std mpsc Sender (allocating a queue block and re-entering the allocator), then parks in `recv_timeout(5s)`; the window is armed by two allocation internals at once and its own nested allocations are charged to the same heap counters the sibling budget row reads. | Reject (tracked at rfs-m62j) | N/A — no functional change this round. The pause site now carries a comment recording the mechanism, the intended `test-support` seam at `serialize_facts`, and why it was deferred. | Deferred, not dropped: the seam needs edits to `Cargo.toml` and `src/lib.rs`, both outside this round's frozen file ownership. Related: rfs-1e6h. |
| F15 | `github://` mutation still answers UnsupportedProjection under a justification comment the PR invalidated, while sibling read-only mounted families answer UnsupportedMutation. | PR #14 review (max) | Verified | The mutation arm returned `github_family_unreadable()` (UnsupportedProjection) while the read arm it named dispatches to the mounted adapter, and the sibling `pr://…/facts` route already answers `unsupported_mutation` end to end. | Accept | Repair R7: the mutation arm now returns a static UnsupportedMutation refusal decided without consulting the mount, and the false comment is rewritten. Fences updated: compiled_sources_contract.rs and github_adapter_contract.rs pin UnsupportedMutation for the write while the search arm keeps UnsupportedProjection. | The search arm is unchanged: there is no unsupported-discovery category and its comment is still accurate. |
| F16 | The comment surviving the deleted Source arm states the opposite of the code: it claims a spelling is refused before the mount is consulted. | PR #14 review (max) | Verified | The arm it annotates ends in `self.github_source()?`, which IS the mount lookup; `GithubAddress` has exactly two variants and no unserved spelling remains; the PR's own rewritten fences assert SourceUnavailable unmounted and DeploymentIdentityUnavailable mounted. | Accept | Repair R7: the comment now states the true contract — reads reach the configured source for every acquired route, and only discovery and mutation are decided without the mount. |  |
| F17 | `validate_entry_links` expects a submodule entry to carry `git/commits/{sha}` in the containing repository, and the fixture invents exactly that url. | PR #14 review (max) | Verified | The generated expectation was `repos/<owner>/<repo>/git/commits/<sha>` for `type: "commit"`, which is definitionally wrong — a submodule's commit belongs to another repository — and a fresh read of `api.github.com/repos/git/git/git/trees/master` confirms GitHub omits both `url` and `size` on such entries, so the fixture's synthesized url is a shape the provider never sends. | Accept | Repair R2: `validate_selected_link` returns Ok for the commit disposition instead of asserting a containing-repository URL. The fixture keeps its submodule entry as the real shape is exercised through the omitted-url path, which is now asserted by the verdict table's `OmitTreeUrl` row. | No mutation covered entry-level links at all before this round, so the whole surface was unfenced; the selected-entry fence now covers opaque, query-bearing and absent top-level links. |
| F18 | Cache-generation invalidation is inexpressible by the retention predicate: it is raised as SourceUnavailable/UpstreamUnavailable, identical to an ordinary outage. | PR #14 review (max) | Verified | The 304 revalidation branch raised UpstreamUnavailable, and the whole read was refused only indirectly by the post-serialization `check_generation`, so the predicate named for retention contributed nothing for that case. | Modify | Repair R1: `ErrorReason::CacheGenerationChanged` was added to the shared vocabulary (DESIGN.md updated in the same commit), raised by `fetch.rs`'s revalidation branch, `check_generation` and `accept_generation`, and refused explicitly by the blob-stage allowlist. Fences updated to the new reason: the generation-change tests in both contract files and the C8 row. | The reviewer's obvious repair — broadening the rejection set to UpstreamUnavailable — would have broken the intended 503 retention, because a plain outage and an invalidation were byte-identical; new vocabulary was required. |
| F19 | `validate_commit` returns parent indices that are always `0..len`, three of four returns are discarded, and `read` re-resolves each index through an error branch that cannot fire. | PR #14 review (max) | Verified | The loop pushed every index unconditionally; `acquire` bound three returns to `_`; `read` re-derived the parent vector and applied `.get(index).ok_or_else(UpstreamMalformed)`, which cannot fire, replacing a construction-time guarantee with a convention. | Accept | Repair R6: the index vector and the `AcquiredCommit.parent_indices` field are gone, `validate_commit` returns only the verified identity, and `read` publishes the same parent slice that loop validated, in the same order, with no re-resolution. | **Corrected by the first re-review**: the round first recorded `read` as consuming a returned slice, but `validate_commit` is called from `acquire`, so no such slice was reachable. The follow-up removed the dead return instead of inventing a caller; the published set and order are unchanged and fenced.
| F20 | Thirteen structurally different identity mutations assert only `details().is_some()`, pinning neither category nor reason. | PR #14 review (max) | Verified | The loop listed thirteen variants and asserted only that details were present; the listener was discarded so no request count was pinned either. | Modify | Repair R11: the loop is now a table of (mutation, category, reason, request count) for fourteen mutations including the new absent-url case, with an assertion that the request count decides whether the walk reached the blob. | Two of the reviewer's illustrative consequences were wrong: an Ok-publishing regression does fail `expect_err`, and WrongBlobSha's reason was already pinned by a sibling test. |
| F21 | The PR dropped the last ErrorCategory assertion on any `github://` read. | PR #14 review (max) | Verified | Repo-wide there were exactly two `DeploymentIdentityUnavailable` test hits, neither asserting `.category()`; changing the production category to SourceUnavailable would have kept the suite green while contradicting DESIGN.md:196 and docs/operating.md:169. | Accept | Repair R11: both the commit and source spellings now assert `ErrorCategory::UnsupportedProjection` alongside the existing reason assertion. |  |
| F22 | The per-dimension intersection gate cannot cover maxDecodedBytes because it reads PR Facts, which never emit it. | PR #14 review (max) | Refuted | `source_facts_apply_decoded_limit_before_and_after_blob_acquisition` sets an operator decoded ceiling of 12 with a caller default of 4 MiB and asserts both the refusal reason and the reported limit, so an intersection regression in the decoded dimension fails today; the MCP row covers the caller-wins direction. | Reject | N/A — no coverage repair. The gate's own doc comment now records that PR Facts are five-dimensional by contract and names the source-side test that covers the sixth dimension (R11). | The reviewer's stated reason for the gap was also wrong: `intersect` takes one argument, not a sixth argument slot. |
| F23 | The new production budget gate measures a 1-segment path against a 1-entry tree, so it certifies the cheapest possible shape. | PR #14 review (max) | Verified | The gate read `…/source/<sha>/payload/facts` against a one-entry tree and asserted four requests, so the extra tree round-trips, the per-entry validation and the wide-tree reconstruction never ran. | Modify | Repair R11: the existing ignored release-only budget test gained two measured rows — the widest admitted tree (1000 entries) and the deepest admitted path — the latter asserting the typed attempts refusal, 10 upstream requests and zero blob requests. | Corrections applied: the tree path allocates with `with_capacity(entries.len())`, not 1000; `160 MiB is 38x loose` was unmeasured; and the ignored-budget inventory does run in CI. |
| F24 | The PR's one added line puts `http/read.rs` at exactly its growth tripwire, so the next edit of any kind fails CI. | PR #14 review (max) | Verified | Reproduced: 350 lines at head against 349 on main, ceiling 350, comparison strict `>`; appending a single blank line made the placement gate exit 1 with two failures (the C12 growth tripwire and the C1 historical child tripwire). | Accept | Repair R10: both pinned literals in `scripts/module_shape_base.py` (LIMITS`[READ]` and `HISTORICAL_CHILD_LIMITS`) are raised to 360, in the pinned policy rather than through a ledger relaxation. | The reviewer named only one of the two ceiling literals; raising one alone would have left the gate red. The quoted `lines=350 maximum=350` line is the success observation, not failure text. |
| F25 | The operator-facing acquisition paragraph still lists five dimensions and omits immutable source Facts from the Resources that accept controls. | PR #14 review (max) | Verified | docs/operating.md:186 listed five hard defaults and ended its control-accepting list at immutable commit Facts, while the ratified table defines six dimensions and source Facts are in the accepted set. | Accept | Repair R9: the sentence now lists the 4 MiB decoded default, notes that only a decoding Resource echoes it, and names immutable source Facts among the control-accepting families. | The reviewer's "undocumented" and "the one doc page they read does not mention it" claims are wrong: docs/operating.md:222 exemplifies the dimension on the source route and :230 documents its default. The defect was a two-item omission from one sentence. |
| F26 | The `github://` catalog entry crams two grammars into one field, and the source spelling's hardest rule gets no worked example. | PR #14 review (max) | Verified | The grammar joined the two spellings with " | " and the example was the commit spelling only; `grammar` is unvalidated free text; the only assertion was that the catalog text contains `/source/`. | Modify | Repair R7: the single entry's grammar now spells the alternatives with sibling `[a|b]` brackets and states the per-component encoding rule with a worked sample from docs/operating.md. Fence: the mcp catalog test now also asserts the encoding rule is stated (R11). | A second catalog entry is not available: `normalize_source_entries` rejects duplicate scheme labels. The "ambiguous field separator" claim is wrong — the renderer joins fields with an em dash. |
| F27 | The new forbidden-token fence for `facts/object.rs` omits six tokens its sibling validation module fences, and the gix tokens are fenced only in two files. | PR #14 review (max) | Verified | The object.rs row carried 13 tokens against identity.rs's 17; the raw delta is nine, of which six are build-safe to forbid (Deserialize/Deserializer cannot be, since object.rs legitimately imports them, as does retry_available's neighbour). module_shape.py's regex over masked production text is the only enforcer. | Modify | Repair R10: `Serialize`, `Serializer`, `serde_json`, `finish_facts`, `CacheMetadata` and `SessionCacheWrite` are appended to the object.rs row. | The second half was rejected: the gix containment that exists is package-level (the architecture test's positive assertion), so moving hashing within the adapter loses no advertised boundary — that is hardening, not a defect. |
| F28 | The boundary fixture router regenerates the 4 MiB payload on every blob request and copies the 5.6 MB base64 string two or three more times, inside a non-ignored test. | PR #14 review (max) | Verified | `boundary_blob` encodes a fresh payload per hit; `replace_api` runs `str::replace` over every string including the 5.6 MB base64, where `@` cannot occur, allocating a full copy; serialization and one mutation add more. | Modify | Repair R11: the marker substitution is now skipped for strings that cannot contain the marker, turning a 5.6 MB copy into a scan, with fixture behaviour unchanged. | The reviewer's closing claims are wrong in both directions: the ignored production budgets DO run in CI (single-threaded, release), and the budget's allocator and wall windows do include fixture-side work. |
| F29 | `gix-object` + `gix-hash` drag in 28 new transitive crates to compute two SHA-1 hashes. | PR #14 review (max) | Verified | Measured from the lockfiles as a set difference against origin/main: 28 packages added, 0 removed. The added stack is unreachable — the jiff datetime chain hangs off gix-actor/gix-date, which this workspace never uses, and prodash arrives through gix-features' progress feature from both crates. `default-features = false` cannot prune: gix-hash declares `default = []`, gix-object declares none. | Reject (tracked at rfs-eqe8) | N/A — no swap this round. | Deferred, not dropped: the fix is a self-contained supply-chain change, but its scope is larger than the review stated (the tree model, Git name ordering and BString handling come with the two hash calls), and hand-rolled identity-critical encoding deserves its own review round rather than riding a 50-finding repair. The reviewed gix usage was verified correct and is fenced by real-SHA fixtures. |
| F30 | `validate_entry_links` performs ~6 allocations and two URL parses per tree entry for a field that is never retained or published. | PR #14 review (max) | Verified | Per entry the loop built a format! suffix, the endpoint join, a parse of the entry url and two origin() comparisons; url 2.5.8's origin() allocates a scheme and a host string, so the true cost is ~9-11 allocations per entry. `VerifiedEntry.url` has exactly one reader and is never published. | Accept | Repair R2: only the selected entry's link is validated. The tree hash reconstructed immediately before authenticates the whole entry set, and every other entry's link is never read, retained or followed. | The reviewer's "never dereferenced" phrasing contradicted its own cited read site; never *published* is the accurate and decisive statement. |
| F31 | `validate_blob` allocates a full 4 MiB decoded buffer that is dead the instant it is hashed. | PR #14 review (max) | Verified | The response body, the compacted base64 String and the decoded buffer are all live simultaneously in `validate_blob`; `hash_object` was already an `io::sink` streaming sink, so the decoded buffer existed only for a length re-check the exact-size proof already established. | Accept | Repair R8: the blob hash streams from a base64 `DecoderReader` through the same gix_hash sink, with the loose header written first and the final-quantum canonicality proof unchanged; no decoded copy is retained. The peak-heap and -27% figures were arithmetic, not measured, and are not claimed here. | The gate that measures this shape has a 160 MiB ceiling, so the finding was a genuine but low-severity allocation win, not a failing budget. |
| F32 | `validate_tree` materializes a full VerifiedEntry for all 1000 entries when the caller consumes exactly one, copying each name a second time. | PR #14 review (max) | Refuted | The VerifiedEntry fields are moved out of the deserialized native entry, not copied — only `BString::from(name.as_bytes())` duplicates bytes, and that copy is inherent to the gix Tree API the hash needs. The "~42,000 across a 7-segment path" figure is cumulative, not live: each tree's entries are dropped as the walk advances. | Reject | N/A — no change. The proposed restructure would also have removed per-entry link confinement, which was the check this PR added; the per-entry URL cost the review missed is repaired under F30 instead. |  |
| F33 | `acquisition_at`, documented as one observation shared by measurement and final emission, hard-codes `max_decoded_bytes: None`, repaired by one caller after the fact. | PR #14 review (max) | Verified | Nine call sites, exactly one of which binds the value mutably and patches the field; the field is `Option` under `skip_serializing_if`, so a forgotten patch produces a valid envelope omitting a dimension the read enforced. | Modify | Repair R3: `acquisition_at` takes the decoded bound as a parameter, the shared no-decode wrapper passes None, and the decoding family names it at its own call site. The `mut` binding and the post-hoc field write are gone. | The reviewer's second option (always emit six dimensions) would reverse DESIGN.md:374's explicit five-dimensional promise; it was not taken. |
| F34 | `MAX_TREE_ENTRIES` duplicates the exported core constant `MAX_COLLECTION_RECORDS` and reports breaches under the same kind. | PR #14 review (max) | Verified | Both constants are 1,000 and the tree breach tags the same `CollectionRecords` kind the collection family uses, so the contradiction the review describes is latent — activated only when one constant is edited. | Accept | Repair R8: the private constant is deleted and the entry ceiling uses `resourcefs_core::MAX_COLLECTION_RECORDS`; the reported kind is unchanged. |  |
| F35 | Hand-rolls the LimitDetail + ResourceError chain that `collection::local_limit_error` already encapsulates. | PR #14 review (max) | Verified | The sibling helper had an identical body and two call sites; the object.rs copy used `?` where the helper uses `expect`, converting a proven-positive bound into a second untested error path. | Modify | Repair R4: `local_limit_error` gains a message parameter and `pub(super)` visibility, and the object.rs site calls it. Fence: the tooWide contract case still asserts the refusal. | The helper now takes the message so each family keeps its own sentence rather than inheriting the collection wording. |
| F36 | `validate_entry_links` builds the expected object URL with the fetch-URL constructor instead of `identity::expected_object_url`. | PR #14 review (max) | Verified | The two joins are byte-identical but their error arms differ; the divergence is unreachable, because `GithubRepositoryIdentity` admits only ASCII alphanumerics plus `._-`, so the join cannot fail for either constructor. | Modify | Repair R2: the selected-entry check uses `identity::expected_object_url`, as commit.rs already does. | Consistency, not an observable defect — the review's behavioral claim was unreachable. |
| F37 | The manual `Serialize for SourceContent` matches `self` three times for one object, with an inner `_ =>` fallback that can be silently forgotten. | PR #14 review (max) | Verified | The field-count match, the body match and an inner reason match inside the shared unsupported arm; a new variant routed into that arm serializes with the wrong reason and no compiler complaint, and the declared count is inert except at zero. | Accept | Repair R3: the serializer now uses `serialize_map(None)`, the count match is gone, and the shared unsupported arm is split so every variant is named exactly once and a new one is a non-exhaustive-match error. Verified byte-identical output for all five states. | **This repair was recorded as applied before it was: the first re-review found the serializer unchanged at HEAD while the record asserted a fix that did not exist. The follow-up round applied it, so the claim now matches the code** (see the re-review section).
| F38 | `validate_tree` destructures the same Presence twice with unreachable arms, makes a second pass for duplicate names, and ends with a no-op `drop(names)`. | PR #14 review (max) | Verified | `drop(names)` is not a borrow-check requirement (std's HashSet newtype declares no Drop, and hashbrown's is `#[may_dangle]`), but the proposed fold-in is a borrowck error — the set borrows `verified` while the loop still pushes into it — and reusing the sort for duplicate detection is unsound because gix orders trees with a virtual `/` suffix. | Modify | Repair R8: the no-op `drop(names)` is deleted; the uniqueness pass stays where the borrow checker requires it. | The reviewer's other two remedies were refuted: one does not compile, the other would drop duplicate detection. |
| F39 | `exact_decoded_size` ends with a six-line fallible checked-arithmetic chain in which no link can fail. | PR #14 review (max) | Verified | Arithmetic over the in-function guards: the divisor is the constant 4, the length is a proven multiple of 4, `padding <= 2` is enforced, and non-zero padding implies at least one full quantum. The `malformed()` it returns is unreachable by any input. | Accept | Repair R8: the tail is `Ok(encoded.len() / 4 * 3 - padding)` with a comment naming both guards; the function keeps its Result for the earlier guards. |  |
| F40 | `read` carries `terminal` as an Option set inside the descent loop and unwraps it with an error arm that can never fire, attributing a local invariant violation to the provider. | PR #14 review (max) | Verified | The loop always assigned on its final iteration because `GithubSourcePath::new` refuses an empty segment list, so the Option was provably always Some and its `ok_or_else` would have blamed GitHub for a local invariant. | Accept | Repair R2: the walk is split with `split_last()` — ancestors in a loop, the terminal segment once after — so `terminal` is a plain value and both the Option and the `index + 1 != len` test are gone. | The empty-segments case is excluded by the type's only constructor, so the `expect` on `split_last` documents a type invariant rather than a panic path; the reviewer's warning not to substitute `expect` on the *terminal* value is respected. |
| F41 | `GithubAddress` exposes no `repository()` accessor, so the PR widens the outer match and adds an inner exhaustive variant match to reach a field both variants carry. | PR #14 review (max) | Verified | Three sites listed the variants; `parse_github_address` mandatorily strips `/facts`, so `ResourceAddress::Github(_)` is extensionally the two-variant list, and a third variant added without updating the census would answer acquisition_controls_unsupported for a supported read. | Accept | Repair R7: `GithubAddress::repository()` is added beside `canonical_reference()` in core, `facts_resource` uses it, and the adapter's `is_facts` collapses to `ResourceAddress::Github(_)`. |  |
| F42 | `decoded_limit` adds a fifth hand-written `LocalLimit` literal; the shared wire type has no constructor and nothing type-checks the kind. | PR #14 review (max) | Verified | Five literals existed, one canonical inside `failure_facts` and four outside; `kind` is a raw `&'static str` a call site could hand-type to diverge from the limit vocabulary. | Accept | Repair R4: `LocalLimit::new(kind: AcquisitionLimitKind, bound, observed)` is the one construction path and the fields are private, so an out-of-module site cannot supply a hand-typed kind. Fence: the collection contract rows assert the same wire values as before. | Covered by R4 rather than by a separate repair; the review's own fix note observed that field visibility is what makes it a type check. |
| F43 | `check_generation` takes the session's global admission mutex once per path segment, three lines before the fetch takes the same mutex to read the same value. | PR #14 review (max) | Verified | The in-loop pre-check's window is a strict superset of the post-fetch check's, so it changes no outcome; for a 7-component path that is 7 redundant lock reads, and the 8th call is the mandated post-serialization publication guard that every sibling family also has. | Modify | Repair R2: the pre-check moves into `read_tree_entry` with a comment stating its actual purpose — refusing before the attempt is charged, so an invalidated read spends none of the shared budget. The calls are retained. | The reviewer's "8 redundant round-trips" overcounts by one, and its own suggested fix (read once before the loop) would have deleted the check entirely, since the baseline is None before the loop. |
| F44 | The final base64 quantum is decoded twice, `compact` retains its newline slack for the life of the resource, and the post-decode length re-check is unreachable. | PR #14 review (max) | Verified | The 4-byte tail decode is a deliberate canonicality proof above the cap, the length re-check cannot fire for input that reaches it, and `String::retain` never shrinks so stripped newlines stay as slack in the published string. | Modify | Repair R8: the streaming hash removes the redundant whole-payload decode and the now-dead length re-check in one step. The 4-byte final-quantum proof and `exact_decoded_size` are kept — that check is load-bearing above the cap. The retain slack is not "fixed": the published `bytesBase64` is the newline-stripped encoding, and shrinking would copy megabytes to reclaim kilobytes. | The reviewer's claim that all three disappear under the streaming rewrite is wrong for two of them; the slack magnitude is also unevidenced in-tree — the repo's own maximum-size fixture emits a single newline. |
| F45 | Two ~60-line concurrency harnesses are line-for-line identical except for eight lines. | PR #14 review (max) | Verified | Both tests built the same Notify + mpsc + Arc<Mutex<Receiver>> + spawn + double-timeout scaffold by hand, and the revalidation-counter prologue was duplicated again in two more tests. | Accept | Repair R11: a `ResponseBarrier` type owns the parking scaffold and a `counting_router` helper owns the per-target revalidation counter; both concurrency tests and both revalidation tests now use them. | The reviewer's "identical except eight lines" understated the drift: the control read sat inside one loop and outside the other, and the harnesses blocked four versus two targets. |
| F46 | `immutable_source_budget_fixture` constructs its oracle twice for one value, first with a placeholder `expected_base64`. | PR #14 review (max) | Verified | The first literal ended in `expected_base64: String::new()` and was overwritten by a functional-update literal once the payload was encoded; only `decoded_size` was read between the two. | Accept | Repair R11: locals are bound first and the oracle is constructed exactly once with every field real. |  |
| F47 | Third byte-identical copy of `host_from_head` in the sources test tree, while the shared support module that owns `FixtureRequest::head()` has no such helper. | PR #14 review (max) | Verified | Three copies existed and all three files already mount the shared TLS support module; `FixtureRequest` owns `head()` and now `host()`. | Accept | Repair R11: `FixtureRequest::host()` is added to the shared support module and all three copies are deleted, with every call site migrated. | The reviewer's call-site count was six, not seven; no copies remain. |
| F48 | `git_object_dependencies_stay_in_source_adapter` is the fourth copy of the cargo-metadata preamble and hard-codes one dependency pair. | PR #14 review (max) | Verified | Four identical preambles existed across eight `MetadataCommand` calls, and the fence was per-dependency-name, so the next native-format codec would be covered by nothing until someone appended literals. | Modify | Repair R10: one `workspace_dependencies()` helper backs the shared preamble, and the gix fence derives its forbidden set from the sources crate's own `gix-` prefixed dependencies, checking both crate edges and underscore tokens in core and MCP, with the positive control kept so the derived scan cannot be vacuous. | The reviewer's "driven from the manifest" was underspecified — Cargo.toml has no codec marker — so the implementable prefix rule over the sources manifest is what landed. |
| F49 | `write_source_profile` inlines the GitHub source row verbatim instead of calling the helper defined 50 lines above it. | PR #14 review (max) | Verified | The helper emits exactly those six keys and two other profile writers already call it; the third re-typed them, risking silent schema drift that only a live run would surface. | Accept | Repair R11: the profile now calls `github_source("dwalleck/resourcefs", None)`; the helper's doc comment no longer says "both". |  |
| F50 | The PR adds two more literal rows to `EXCLUDED`, a list that grows by construction on every increment. | PR #14 review (max) | Verified | Twenty entries, all but two child-server hosts matching the `live_` naming convention AGENTS.md mandates, and only BUDGETS carried a completeness check, so forgetting a row failed CI with a generic unclassified-test error. | Modify | Repair R10: `EXCLUDED` becomes a predicate — the two convention-breaking child-server hosts stay explicit with their reasons, and the `live_` convention covers the rest — evaluated only for names not in BUDGETS, which stays a literal inventory with its completeness check. | The reviewer missed that the membership test ran before the budget check, so a name in both sets was silently skipped and never executed; the rewrite closes that. |

## PR #14 compact repair records

Each record names the atomic repair, the findings it resolves, the paths it changed, and the verification that observed it.

| repair | findings | root-cause change and affected paths | commands and observed results | evidence disposition |
|---|---|---|---|---|
| R1 blob-stage retention policy | F1, F2 (surfacing), F5, F6, F18 | The source family borrowed the collection engine's page-rejection predicate, so one boolean answered "retain a partial result" and "reject the read" from an allowlist whose default arm was retention. `facts/source.rs` now classifies with an exhaustive allowlist of ordinary request failures; `core/error.rs`, `github/fetch.rs`, `facts.rs` and `facts/commit.rs` give cache-generation invalidation its own reason. | `cargo test --workspace --all-features` green (79 test binaries); `python scripts/ci-gates.py` exit 0. Fault injection: replacing `retains_verified_metadata` with `true` turns 5 fences red (`source_facts_refuse_the_whole_read_for_a_deadline_refusal`, `..._when_the_response_ceiling_bounds_the_blob`, the verdict table, the deep-attempt row, the fixture-case table); restoring turns them green. | Invalidated: the prior `BlobUnavailable`-only retention row and the `deepBlobLimit` fixture expectation. Replacement: the three-way ordinary/refusal retention rows and the typed attempts refusal. Retained: the identity/malformed refusal coverage, now with pinned category and reason. |
| R2 descent and selected-entry validation | F8, F10, F11, F17, F30, F36, F40, F43 | The descent loop carried an always-Some Option and applied a strict link validator to every entry of every traversed tree, with a submodule expectation that named another repository's object. `facts/source.rs` splits the walk with `split_last`, validates only the selected entry with the documented deployment constructor, and classifies a non-directory ancestor as not-found; `facts/identity.rs` makes the optional-link rule match its contract. | Full suite green. Fences: `source_facts_classify_mid_path_and_absent_components_as_not_found` (3 ancestors plus the absent name, request count pinned), `source_facts_read_a_path_whose_sibling_entries_carry_opaque_links`, and the `OmitTreeUrl` row of the verdict table. | Invalidated: the whole-tree `validate_entry_links` coverage, which had no fence at all (no mutation ever altered an entry link). Replacement: the selected-entry fence above. Retained: the required tree/blob link checks, unchanged in verdict. |
| R3 presence-aware terminal size and provenance | F4, F12, F33, F37 | The supplied tree-entry size was published as an observed limit value, the terminal size collapsed null and omitted, `unavailableFacts` was a constant empty list, and the shared acquisition constructor hard-coded the decoded bound for one caller to patch afterwards. `facts/source.rs`, `facts.rs` and the `SourceContent` serializer. | Full suite green. Fences: `source_facts_refuse_a_size_refusal_without_claiming_an_observed_size`, `source_facts_publish_presence_aware_terminal_size`, and the existing envelope-shape rows in the source and mcp contracts. | Invalidated: the decoded-size-limit row that asserted `limit.observed` equal to the supplied size. Replacement: the same refusal asserted with `observed` absent. Retained: every decoded-cap boundary row, unchanged. |
| R4 shared limit vocabulary | F35, F42 | Four out-of-module sites re-typed the wire limit literal with a raw `&'static str` kind. `facts/collection.rs` gains the one constructor and loses field visibility; the limit-error helper takes the family's own sentence. | Full suite green; the collection and tree limit contract rows assert unchanged wire values. | Retained: both existing limit-error call sites and their contract coverage; only the construction path changed. |
| R5 blocking verification worker | F13 | Multi-megabyte base64 decode, SHA-1 and tree reconstruction ran inline on the polled read future against the repo's own `spawn_blocking` convention. `facts/source.rs` offloads `validate_tree` and `validate_blob`. | Full suite green; the request sequence and every document byte are unchanged, which is what the contract rows assert. | Retained: all existing tree/blob contract rows; the change moves work between threads, not between requests. |
| R6 commit parent pairing | F19 | Publishing re-resolved validated parents through an index vector and an error arm that could not fire. `facts/commit.rs` drops the index vector and the `AcquiredCommit.parent_indices` field and publishes the same parent slice the validation loop walked. | Full suite green; the commit contract rows assert the same parent SHAs and links. | Retained: commit parent coverage; the removed field had no consumer outside the file. |
| R7 compiled-source surface | F15, F16, F26, F41 | The write path answered an unserved-route projection error, its read-arm comment claimed a pre-mount refusal, the catalog crams two grammars with no worked example, and three sites re-listed an address's variants. `sources/compiled.rs`, `github/mod.rs`, `core/reference/github.rs`. | Full suite green. Fences updated and discriminating: `compiled_sources_contract.rs` and `github_adapter_contract.rs` (write now UnsupportedMutation, search still UnsupportedProjection), the mcp catalog row now also asserts the encoding rule. | Invalidated: the two assertions that pinned the old projection category for the write. Replacement: the mutation-category pins. Retained: the discovery refusal, unchanged. |
| R8 object validation cost and dead code | F31, F34, F38, F39, F44 | A whole decoded payload was allocated only to be hashed and dropped, a private ceiling duplicated a core constant, an unreachable checked-arithmetic chain advertised four failure modes, and a no-op drop read as a borrow requirement. `facts/object.rs`. | Full suite green, including every tree/blob hash reconstruction against the fixtures' real Git SHAs. The streaming hash was proved to equal the previous `BlobRef` hash for 15 payload sizes covering all padding widths in a scratch crate on the repo's exact dependency versions. | Invalidated: the post-decode length re-check, which no input could reach. Retained: the pre-allocation cap gate, the 4-byte final-quantum canonicality proof, and the whole-tree uniqueness pass. |
| R9 contract prose | F2, F7 (residue), F25 | The derived depth bound and the whole-read refusal set were unstated, and one operator sentence listed five of six dimensions. `DESIGN.md`, `docs/operating.md`. | The gate's documentation checks and the full suite are green; no behaviour is asserted by these files. | Retained: every existing statement; the edits add the derived bound, the invalidation reason, and the sixth dimension. |
| R10 gate policy | F24, F27, F48, F50 | `http/read.rs` sat exactly on its tripwire, the object.rs token fence omitted six build-safe tokens, the architecture fence hard-coded one dependency pair, and the ignored-test exclusion list grew by literal on every increment. `scripts/module_shape_base.py`, `scripts/module-ledger.json`, `scripts/ci-gates.py`, `mcp/tests/architecture_contract.rs`. | `python scripts/module_shape.py` exit 0; `cargo deny check` clean; the architectural fence's three converted tests pass with `MetadataCommand` calls reduced from eight to five. Fault injection: one appended blank line in `http/read.rs` had made the gate exit 1 with two failures; raised ceilings clear it. | Invalidated: the 350-line read.rs budget. Replacement: 360 in both pinned literals. Retained: BUDGETS as a literal inventory with its completeness check, and the pair of child-server exclusions. |
| R11 fences | F20, F21, F22, F23, F26, F45, F46, F47, F49 | The identity fence pinned neither category nor reason across thirteen mutations, no test pinned the deployment-identity category, the budget gate measured the cheapest shape, and the concurrency harnesses were hand-rolled twice. Test files and the shared TLS support module. | Full suite green. Discriminating: the verdict table fails when its production counterpart is loosened (proved by the R1 injection); the new budget rows assert request counts and the typed refusal; the catalog assertion fails on the pre-repair spelling. | Invalidated: the `details().is_some()` identity loop and the two `deepBlobLimit` expectations. Replacement: the (mutation, category, reason, request count) table and the typed-refusal row. Retained: every other fence in the files, unchanged. |

### Re-review of this round (2026-09-11)

Three adversarial reviewers examined the repair diff itself, because the repairs are new code. The reviewers also confirmed, with reasoning the parent could not refute, that the descent and selected-entry narrowing, the streaming blob hash, the decoded-bound plumbing, the blocking worker, the gate policy and the ignored-test predicate are correct. Every finding below was verified by the parent and is repaired.

| re-review id | area | evidence state | finding | decision | fix |
|---|---|---|---|---|---|
| RR1 | F37 / R3 serializer | Verified | The repair recorded as applied was absent: `SourceContent` still matched `self` three times with the inner `_ =>` fallback, so a new variant could silently serialize with another variant's reason. | Accept | `serialize_map(None)` and a split unsupported arm, verified byte-identical for all five states. |
| RR2 | F19 / R6 parents | Verified | Same class: the record asserted a returned slice that could never reach `read`, because validation happens in `acquire`. | Accept | The dead return and the index vector are removed rather than a caller invented; published set and order unchanged. |
| RR3 | R1 generation vocabulary | Verified | The new distinct reason left four sibling families (`comment.rs`, `inline.rs`, `review.rs`, `collection.rs`) reporting `upstream_unavailable` for the same condition, so one condition answered two ways depending on where the invalidation landed. | Accept | All four return `CacheGenerationChanged`. |
| RR4 | R11 sibling-link fence | Verified | The F30 narrowing's fence could not fail: no fixture entry carried a foreign link, so re-widening validation to every entry left the suite green. | Accept | `ForeignSiblingLink` and `SiblingUrlWithQuery` mutations on an unselected entry. Fault injection re-widening the loop turns the test red; restoring turns it green. |
| RR5 | R11 blob-side presence fence | Verified | `identity::required(&native.url)?` had no fence: deleting it left every test green. | Accept | `OmitBlobUrl` row: SourceUnavailable / UpstreamMalformed at 5 requests. |
| RR6 | R11 retention arms | Verified | `UpstreamRateLimited` and `TransportFailure` were in the allowlist but no test produced either at the blob stage. | Accept | `BlobRetryAfterWithinDeadline` (429 whose guidance fits, retained as `upstream_rate_limited`) and `BlobAborted` (retained as `transport_failure`). Fault injection removing either arm turns the retention test red. |
| RR7 | R11 presence contract | Verified | Only the omitted branch of the terminal size was exercised, and the assertion that appeared to cover the null branch was vacuous — `Value` indexing also yields Null for an absent key. | Accept | A `NullBinaryTreeSize` mutation, present/absent asserted through `as_object`, a positive row for a supplied size, and the vacuous assertion replaced. |
| RR8 | R11 budget row label | Verified | The new deep row is labelled "worst admitted path" while asserting a refusal, so no publishing read at depth is measured. | Modify | The row and its doc comment now state that they certify the attempt-refusal boundary, and record that the corpus has no seven-component publishing case; adding one needs new corpus data. |
| RR10 | R6 doc contract | Verified | A follow-up verification pass found `validate_commit`'s doc still promising "the validated slice the caller publishes from" after that return was removed, so the function's contract and its call site gave opposite accounts of who owns the parent slice. | Accept | The doc now states what the function does: its parent loop validates every parent of the one slice `read` publishes. |
| RR11 | R11 redundant mutation | Verified | `SiblingUrlWithQuery` was a byte-identical duplicate of the pre-existing `QueryBearingEntryLink`, already applied to the same unselected sibling at HEAD, so it could not fail on anything the older row did not. | Accept | The variant and its three match arms plus the test row are deleted; `ForeignSiblingLink` is the row that carries the narrowing's fence. |
| RR9 | R10 commit narrative | Verified | The two `facts.rs` body registrations rode in the gate commit rather than the commit that changed those bodies. | Reject | Narrative only: the tree is consistent and both registrations were verified legitimate (both bodies exist at the pinned baseline and genuinely differ). The follow-up commit records it. |

### Fence-mutation receipts for this round

Every new or changed fence this round was shown red under an injected fault on the repaired tree, then green once restored. Each row names the mutant, the one artifact it edits, and the assertion that turned red.

| mutant | edit | fence turned red |
|---|---|---|
| X1 | `retains_verified_metadata` returns `true` (retain everything) | Five fences, one run: `source_facts_refuse_the_whole_read_for_a_deadline_refusal`, `..._when_the_response_ceiling_bounds_the_blob`, the verdict table, `source_facts_keep_one_attempt_budget_through_deep_blob_stage`, and `source_facts_cover_every_independent_fixture_case`. |
| X2 | `read_tree_entry` validates every entry of the traversed tree instead of the selected one | `source_facts_read_a_path_whose_sibling_entries_carry_opaque_links` — panics on `ForeignSiblingLink` with `source_unavailable`, which is the narrowing this fence exists to protect. |
| X3 | only the `ErrorReason::UpstreamRateLimited` arm removed from the retention allowlist | `source_facts_retain_ordinary_blob_failure_but_not_identity_failure` — panics on the `BlobRetryAfterWithinDeadline` row. |
| X4 | only the `ErrorReason::TransportFailure` arm removed | The same test — panics on the `BlobAborted` row. Run separately from X3 because X3's loop aborts on its first failing row, leaving the transport row unexercised. |
| X5 | `identity::required(&native.url)?` deleted from `read_blob` | The verdict table — `OmitBlobUrl` degrades from `SourceUnavailable`/`UpstreamMalformed` to `NotFound`. |
| X6 | `Presence::omitted()` returns true for `Null` as well | `source_facts_publish_presence_aware_terminal_size` — the explicit-null row loses its key. |

Restored green after each: `cargo test -p resourcefs-sources --test github_source_contract` — 17 passed.

Not individually mutated, and recorded as such rather than implied: the `OpaqueEntryLink` and `QueryBearingEntryLink` rows (X2 exercises them only in passing — an opaque value passes even a widened check, which is the behaviour they assert), the positive supplied-size row (proven by the fixture's own value, not by a mutant), and the `BlobUnavailable`/`BlobDenied`/`BlobAbsent` retention rows (X3/X4 cover the allowlist mechanism; these three name their own published category and reason).

### Tracker dispositions

| item | issue | why deferred |
|---|---|---|
| F14 (allocator-as-synchronizer in the C8 window) | rfs-m62j | The seam needs `Cargo.toml` and `src/lib.rs`, outside this round's frozen ownership; the pause site is behaviourally correct today and now records the mechanism and the remedy. Related: rfs-1e6h. |
| F29 (28 packages for two Git object hashes) | rfs-eqe8 | A self-contained supply-chain change whose true scope is larger than the review stated, in the module that authenticates content; deserves its own review round. |
| F2 residual (paths deeper than the attempt ceiling) | rfs-lqat | The refusal is now typed and documented; whether to raise the ceiling, split the ledger, or refuse the address is a design amendment, because the approved design calls deep-path refusal explicit bounded behaviour. |
