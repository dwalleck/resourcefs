# Build ResourceFS as an independent Rust product

ResourceFS will be a standalone Rust workspace and dedicated binary, released independently from Oh My Pi. OMP’s path-shaped tools are design input, but ResourceFS owns a separate versioned Behavior Contract and may diverge rather than sharing implementation or promising continuing OMP conformance.
